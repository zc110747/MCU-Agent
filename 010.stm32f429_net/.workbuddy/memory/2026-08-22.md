# 2026-08-22 STM32F429 + LwIP 网络工程打通

## 成果
裸机 LwIP (NO_SYS) + HTTP server 在真实硬件上跑通。验收全部通过：
- ping 192.168.10.99：0% 丢包，RTT <1ms
- ICMP 全尺寸 32B~16000B（含 11 分片）3/3 通过
- HTTP GET / 返回 200，60 次连续压测 60/60，无 pbuf/pcb 泄漏
- 压测后 CFSR=0 / HFSR=0，PC 稳定在 MX_LWIP_Process 主循环，栈仅用 24B
- ARP 表 MAC = 00-80-e1-xx（ST 的 OUI），确认响应者是本板

资源占用：FLASH 60952 B (5.81% / 1MB)，RAM 64064 B (32.58% / 192KB)

## 两个关键 Bug（都是"推断 vs 实测"的教训）

### 1. 链接脚本内存尺寸错误 → 启动即 HardFault
**症状**：烧写 verify OK，但复位后立刻停在 HardFault_Handler。
HFSR=0x40000000 (FORCED)、CFSR=0x00001400 (PRECISERR+UNSTKERR)、MSP=0x20040000。

**根因**：`STM32F429IGTx_FLASH.ld` 写的是 `RAM LENGTH = 256K` / `_estack = 0x20040000`，
但 STM32F429IG 真实连续 SRAM 只有 **192K**（SRAM1 112K + SRAM2 16K + SRAM3 64K，
0x20000000~0x2002FFFF）。CCM 64K 在 0x10000000，不连续且 ETH DMA 访问不到。
初始栈顶落在未映射地址 → 第一次压栈就总线错误。
FLASH 也写错了（2048K，实际 1024K）。

**实测方法**（别靠推断，芯片手册也可能记错型号）：
```
openocd ... -c "mdw 0x2002FFFC"  # 可读
            -c "mdw 0x20030000"  # ERROR -> 边界在此
```
注意：我第一轮曾推断成 128K，也是错的。只有实测靠得住。

**附带修复**：CMake 没把 .ld 当依赖，改了不会重新链接（`ninja: no work to do`）。
已加 `set_target_properties(... LINK_DEPENDS ${LINKER_SCRIPT})`。

### 2. 硬件校验和卸载破坏 IP 分片
**症状**：`ping -l 1472` 通，`ping -l 1473` 100% 超时。悬崖式失败，正好在分片边界。

**根因**：`CHECKSUM_BY_HARDWARE=1` + `TxConfig.ChecksumCtrl =
ETH_CHECKSUM_IPHDR_PAYLOAD_INSERT_PHDR_CALC`。MAC 靠扫描"当前这一帧"来插入 L4 校验和；
lwIP 分片后每帧只含部分载荷，MAC 对分片算校验和 → 值错误 → 对端静默丢弃。

**排查关键**：先用 `gcc -dM -E` dump 实际生效宏，确认 IP_REASSEMBLY/IP_FRAG 都是 1，
排除配置问题后才锁定校验和。（`#pragma message` 在 `-E` 下被原样透传不求值，不能用。）

**结论**：无法做"按包混合"——CHECKSUM_GEN_* 是编译期全局开关，lwIP 无法只为分片算校验和。
已改为软件校验和（代价 +640B flash，ping RTT 无可测差异），并加 `ETH_USE_HW_CHECKSUM`
单一开关同时驱动 lwipopts.h 和 ethernetif.c——两处脱钩正是本 bug 成因。

## 清理掉的死配置（易误导）
- `IP_FRAG_USES_STATIC_BUF` / `IP_FRAG_MAX_MTU`：lwIP 1.4 遗留，2.x 已移除
- `ETH_RXBUFNB` / `ETH_TXBUFNB`：旧 HAL ETH 驱动的，现代驱动只读 ETH_RX/TX_DESC_CNT
- 实际生效的 RX 缓冲数是 ethernetif.c 里的 `ETH_RX_BUFFER_CNT`(20)

## 环境坑
- Git Bash 吞掉 curl 的 `-w "%{http_code}"` 花括号，输出成 `code=%http_coden`，
  导致压测误判全失败。改用响应体 grep 判定。
- 原生 arm-none-eabi-gcc 看不到 MSYS 的 `/tmp` 和 `/dev/null`，临时文件要放项目内。
- ipconfig 输出是 GBK，需 `iconv -f GBK -t UTF-8`。
- openocd (sysprogs 0.12.0) 用 `transport select swd`，不是 `hla_swd`。

## 调试用符号地址（改代码后会变，需重新 nm）
gnetif=0x20007ed0 (ip_addr 在 +0x4)、link_up_prev=0x200001f0、
eth_rx_ready=0x200001f8、heth=0x20000234

## 22日第二波：FreeRTOS 化重构 + SDRAM（用户要求）
- 用户要求：回退 TLS，FreeRTOS 化 LwIP（third_party/FreeRTOS-Kernel V11.1.0）+ SDRAM 内存池
- SDRAM: bsp_sdram.c（FMC Bank1, W9825G6KH 32MB, 90MHz, 引脚按 stm32f4_hw.md）+ 自检通过
- 内存布局：ram_heap 128KB @0xC0000000 (lwip mem heap)、ucHeap 512KB @0xC0020000 (FreeRTOS heap)
  → 链接脚本 SDRAM region + NOLOAD 段；RX_POOL/memp 池必须留 SRAM（ETH DMA 写 SDRAM 分片丢包）
- LwIP 多线程：NO_SYS=0, LWIP_NETCONN=1, tcpip_init + tcpip_input(ISR) + sys_arch.c(FreeRTOS)
- HTTP: netconn API + 独立任务（先 recv 再响应，否则 close 发 RST）
- HAL 时基 TIM7（SysTick 给 FreeRTOS）
- 排障三大坑（详见 MEMORY.md）：
  1) V11 INCLUDE_* 默认 0 + port.c 需 softfp + 向量表必须直指 port 函数（port.c assert 343）
  2) 调度器前 xTaskCreate 残留 BASEPRI=0x50 → TIM7/HAL_Delay 卡死 → tcpip_init 后 __set_BASEPRI(0)
  3) ISR 里 SYS_ARCH_PROTECT 不能 vPortEnterCritical → xPortIsInsideInterrupt 跳过
- 验收：SDRAM 32MB OK、ping 20/20、ICMP 32~16000B 全通、HTTP 30/30、CFSR/HFSR=0
- Debug/Release 双构零警告；README.md 已更新

## 22日第三波：HTTPS（mbedTLS 3.6.2）+ SDRAM 内存池（用户要求）
- 要求：接入 TLS（mbedTLS），内存用 SDRAM，HTTP+HTTPS 双协议
- 复用 third_party/mbedtls 3.6.2 + 重建 app/mbedtls_config.h（TLS1.2 server, ECDHE-ECDSA P-256）
- mbedTLS 堆池放 SDRAM：app/mbedtls_pool.c 32KB @0xC00A0000（链接脚本 .sdram_mbedtls_pool NOLOAD）
- https_server.c：netconn 443 + mbedTLS 阻塞 BIO（FreeRTOS 下最简单架构，无 WANT_READ 状态机）
- 证书：openssl EC P-256 自签名 CN=stm32f429.local（app/web/cert/ + tls_creds.h）
- **关键 bug**：mbedTLS BIO 的 netbuf_copy 只拷请求量、剩余被 delete 丢弃 → 握手卡死
  （5B 记录头 + body 分段到达时丢失）→ BIO 加内部读缓冲 g_rxbuf 修复
- 验收：HTTP 80 20/20、HTTPS 443 20/20（curl TLS1.3 ClientHello 正确降级）、
  openssl TLSv1.2 ECDHE-ECDSA-AES128-GCM-SHA256、TLS 堆峰值 13376/32768B、
  Debug/Release 零警告（common.h array-bounds 假阳性用 pragma 屏蔽）
- README.md 已更新；scripts/tls_probe.py / tls_probe12.py 交叉验证客户端

## 22日收尾：技能沉淀补漏 + 任务状态清理
- 发现 stm32-bare-metal-bringup 技能缺 HTTPS/mbedTLS 章节（总结说已写入但实际遗漏）
- 技能补 8.9/9.x 章节：netbuf 余量丢失导致握手卡死的坑、mbedTLS 3.x 三件套
  （尖括号配置注入 / MEMORY_BUFFER_ALLOC 需配 PLATFORM_MEMORY / PEM strlen+1）、
  TLS 堆峰值验收法；触发词补 HTTPS/TLS/mbedTLS
- 清理过时任务状态：#2/#5/#7/#9/#10/#12/#13 → completed（工作早已完成）
- 最终确认：HTTP 80 + HTTPS 443 双通、ping 1ms，无回归

## 22日第四波：ERR_CONNECTION_RESET 根因 + HAL_Delay 全清除（用户要求）
- 用户浏览器报 ERR_CONNECTION_RESET（之前能通），curl HTTP/HTTPS 全 RST（exit 56/35）
- 排查：CFSR=0、ping 通、PC 在 LwIP 主循环 → 非 fault 非死锁
- 根因：**BASEPRI 残留复燃**——main.c 在 vTaskStartScheduler 前连续 xTaskCreate
  （tcpip_thread + httpd + httpsd），只清了 tcpip_init 后的，server init 后漏清
  → httpd/httpsd 任务静默未启动 → 端口无 LISTEN → SYN 收 RST
- 修复：每个调度器前 xTaskCreate 后都 __set_BASEPRI(0); __enable_irq()
- 用户要求：不要用 HAL_Delay，用 vTaskDelay → 新增 bsp/bsp_delay.h
  （DWT CYCCNT 忙等，header-only 不改 CMake）；调度器前 3 处 HAL_Delay
  （PCF8574 50ms×2 + SDRAM 1ms）改 bsp_delay_ms/us；任务内本来就是 vTaskDelay
- 应用层 HAL_Delay 零调用达成；Debug/Release 双构零警告（仅 RWX 良性）
- 验收：HTTP 15/15、HTTPS 15/15（DWT 延时下 SDRAM 初始化/PHY reset 正常）
- 技能 8.4 更新（每个调度器前 xTaskCreate 后都要清）+ 新增 8.9（DWT 忙等）

## 22日第五波：HTTPS 8.88s 慢 → ECP 优化（用户报告）
- 用户：浏览器 https 8.88s 才出页面，问是否 TLS 任务优先级问题
- curl 时间分解：TCP 2ms、TLS 握手 4.88s（Debug）/ 3.67s（Release）稳定无波动 → 计算瓶颈
- **不是优先级问题**：httpsd=3 与 tcpip_thread=3 同级时间片轮转不饿死（tcpip 无包阻塞让出）
- 根因：mbedtls_config.h 白名单配置缺 ECP 加速宏 → P-256 通用模约减慢 5-10 倍
- 修复：加 MBEDTLS_ECP_NIST_OPTIM + WINDOW_SIZE=6 + FIXED_POINT_OPTIM=1
- 结果：Debug 4.88→0.83s、Release 3.67→0.64s（约 6 倍）；FLASH +18KB（window6 表）
- 双构零警告（仅 RWX）；README 新增"HTTPS 握手性能"表格；技能 9.5 新增
- 后续可选：MBEDTLS_HAVE_ASM（M4 umull asm）再降 20-40%，需验证 3.x 支持

## 22日第六波：HTTPS 再优化（用户要求更快 1.46s→0.8s）
- 用户实测浏览器 1.46s；固件 curl 单连接 0.79s（Debug）
- 加 MBEDTLS_HAVE_ASM（M4 umull/umlal DSP asm，bn_mul.h ARMv6+ 路径）：0.83→0.79s
- 试 X25519（CURVE25519_ENABLED + mbedtls_ssl_conf_groups 优先）：**反而 1.07s**——
  mbedTLS 3.6 无 Everest，走通用 Montgomery ladder 比 NIST 优化 P-256 慢 → 回退
- **1.46s 真因**：浏览器并发请求 / + /favicon.ico 两个连接，httpsd 单任务串行握手
  （实测 0.80+0.82=1.62s）→ HTML 加 <link rel=icon href="data:,"> 消除 favicon 请求 → 单握手
- 最终：Debug 0.79s / Release 0.64s（P-256 + NIST/WINDOW6/FIXED + ASM）
- 技能 9.5 补充：HAVE_ASM 收益小、X25519 无 Everest 时更慢（实测否决）、favicon 双连接坑

## 22日第七波：Vue 网页端三视图（用户要求，先做前端）
- 用户要求：复杂网页 index.html 默认页，Vue 方案，放 web/ 目录，构建产物拷 SD 卡
- 新建 web/（Vue3 + Vite5）：App.vue 三视图（硬件数据/网络数据/参数修改）
  - 硬件：MCU/时钟/AP3216C(lux,ps,ir)/MPU9250(9轴)/LED/BEEP 1s 轮询 + 控制按钮
  - 网络：IP/掩码/网关/MAC 2s 轮询
  - 参数：IP/掩码/网关/MAC 表单校验+保存、复位设备（轮询恢复在线）
- API 约定（固件下一步）：GET /api/hardware、GET /api/network、POST /api/control
  {led|beep}、POST /api/network、POST /api/reset，全 JSON
- vite base:'./' 相对路径 → dist 可直接拷 SD 卡任意路径加载
- scripts/preview_server.py：本地 mock API 预览（python http.server，DIST 需 normpath 坑）
- 构建产物：dist/index.html 0.38KB + assets JS 72KB + CSS 1.7KB

## 22日第八波：SD 卡网页 + JSON API（用户要求，嵌入式阶段）
- 要求：SDIO 驱动 + FatFs 从 SD web/ 读网页；http server 支持参数读取与实时更新
- 硬件确认（用户）：BEEP=PCF8574 P0 **低电平**发声（P0=L→蜂鸣器导通）；MPU9250 自研移植
- 新增：bsp/bsp_sdio.c（SDIO 4-bit 轮询，PC12/PD2/PC8-11）+ HAL_SD + LL_SDMMC 接入
- FatFs R0.15 对接：diskio.c 重写（对接 bsp_sdio），ffconf 改 FF_USE_LFN=2（assets 长文件名必需）
  + FF_CODE_PAGE=437 + FF_VOLUMES=1 + FF_FS_NORTC=1 + FF_USE_STRFUNC=1（f_gets）
- 传感器：bsp_ap3216.c（I2C2 0x1E 真数据 ✅）、bsp_mpu9250.c（0x68 自研：
  加速度/陀螺仪 ✅，磁力计 AK8963 读出 0 待修）
- web_serve.c/h：统一路由（SD 网页 + /api/* JSON），http/https 双协议复用（send 回调抽象）
- API：hardware/network GET+POST/control/reset；netcfg.c 网络参数持久化 SD netcfg.ini
- 排障：① path 解析未截断 HTTP 版本 → /api/* 全 404（strcmp 失败）② httpd 栈 4KB 溢出
  → HardFault IBUSERR（api_hardware 1.1KB 局部变量 + 深层调用链）→ 栈加到 8KB
- 真机验收：AP3216C lux/ps/ir 实时、MPU9250 az=1.0 重力、LED/BEEP 控制生效+状态回读、
  HTTPS API 通、network 修改回读一致、无卡 fallback flash 页；Debug/Release 双构零警告
- SDIO init FAILED 实测=板子未插 SD 卡（预期行为）

## 22日第九波：SD 卡已插入但 SDIO init 仍 FAILED（诊断中）
- 用户插卡后重启：SDIO: init FAILED (err=0x00000001 CMD_RSP_TIMEOUT)
- 已做固件侧优化：ClockDiv 0x76 低速 init（SD 规范 ≤400kHz）+ 20ms 上电延时 + 2 次重试
- openocd 读 SDIO 寄存器：POWER=3(ON)、CLKCR=0x176(分频 0x76 正确) → 外设正常
- 结论：SDIO 外设/时钟/引脚配置正常，卡完全无响应 = **物理层问题**
- 待用户检查：① 卡插入是否到位（咔哒锁住）② 卡槽 VDD(4脚) 是否 3.3V（供电）③ 换卡排除
- 磁力计按用户要求暂不修

## 22日第十波：SD 卡正常后功能验证（ST-Link 仿真 + 串口/网络）
- 用户：项目工作正常表示 SD 卡正常，在此基础上验证功能；可用 ST-Link 仿真 + 串口验证
- 环境确认：openocd @ /d/software/ST/OpenOCD/bin/openocd（sysprogs 0.12.0，swd，1800kHz）
  gdb @ E:/support_tools/arm-gnu-toolchain-15.3.../bin/arm-none-eabi-gdb
  python venv pyserial 3.5 已装（隔离 envs/default）
- 新增脚本：
  - scripts/flash.sh：ST-Link 一键烧录（openocd -f openocd.cfg + program verify reset；
    先 kill 残留 openocd 防 LIBUSB_ERROR_ACCESS；路径用 pwd -W 转 Windows 风格给原生 openocd）
  - scripts/verify_all.py：5 模块验证 HTTP/HTTPS/API，pass/fail 计数 + 可选 --serial COMx 抓日志
  - scripts/deploy_web.py：把 web/dist/ 部署到 SD 卡 web/ 目录（自动探测盘符）
- 验证结果（板子已烧录运行）：
  - ✅ /api/hardware：lux/ps/ir + MPU9250 9轴（az=1.0 重力）JSON 正常
  - ✅ /api/network GET：ip/mask/gw/mac 返回正确
  - ✅ POST /api/control：led/beep on→ok→复位关闭（避免一直响）
  - ✅ POST /api/network persist：返回 ok 写 SD netcfg.ini
  - ❌ HTTP/HTTPS 首页来自 flash fallback（非 SD web/）：SD 卡 web/index.html 缺失
  - ST-Link 仿真：gdb 加载 elf 符号读 g_netcfg（默认 192.168.10.99...）/g_led_on=0/g_beep_on=0，
    FreeRTOS 已在跑（停在 prvIdleTask）
- 发现：SD 卡 web/ 目录没拷网页产物（之前只确认 SDIO mount OK 没部署）。需断电拔卡→PC 拷
  web/dist/* 到 SD web/→插回上电，固件才会从 SD 读 Vue 页
- 判定逻辑修正：SD 页特征 `<div id="app">`/`./assets/`；fallback 特征 `STM32F429IGT6 Web Server`

## 第十一波：放弃 SDIO，网页改编译期 const 数组
- 用户决定放弃 SDIO/FatFs 方案（SD 卡接触不良，后续修复再升级）。传感器（AP3216C/MPU9250）保留
- 新增 `app/web/web_assets.c/h`：把 `web/dist/` 3 文件（index.html 388B / css 1657B / js 72504B，共 74549B）
  用 Python 脚本转 const unsigned char[]，定义 `web_asset_t{path,data,len,ctype}` 资源表
- `web_serve.c`：删 `#include ff.h`/`WEB_ROOT`/`send_file(FatFs)`/`content_type_of`，改 `send_asset()` 从内存表按 URL 查找；
  GET / → /index.html；找不到才 fallback flash 页。新增 `#include web_assets.h`
- `netcfg.c/h`：去 ff.h 与 0:netcfg.ini 读写，load 返回 0、save 返回 1（内存-only，API 兼容）
- `main.c`：删 bsp_sdio.h/ff.h、g_fs、SDIO init 块、f_mount/netcfg_load 调用
- `CMakeLists.txt`：移除 FATFS_SOURCES、hal_sd.c、ll_sdmmc.c、third_party/Fatfs include
- `stm32f4xx_hal_conf.h`：关 HAL_SD_MODULE_ENABLED
- 删除 bsp/bsp_sdio.c/h
- **双构零警告**（仅 RWX segment 链接器提示，历史已有非代码警告）
- 资源占用：Flash text 239844B+data 184B ≈ 240KB / 1024KB（~78% 余量）；web_assets 在 Flash 不占 RAM
- ST-Link 烧录 Verified OK；`verify_all.py` 6 项全 PASS（HTTP/HTTPS 首页来自 const 数组，非 fallback）
- 注意：verify_all.py 输出标签仍写"SD Vue page"，实际已无 SD 依赖，标签命名遗留

## 第十二波：修复 netconn_write 大文件截断
- **现象**：浏览器访问 https://192.168.10.99/ 只显示深色背景（CSS 的 --bg 已生效但 JS 不渲染）
- **根因**：72.5KB JS 文件被截断到 6968B；CSS 1657B 完整（小文件不触发）
  curl 报 `curl: (18) end of response with 65536 bytes missing`
- **诊断**：lwnet `netconn_write(NETCONN_COPY)` 在内存压力下返回 ERR_MEM 或只写部分，
  http_send / https_send / https_bio_send 一次调用没循环，直接关闭连接
- **修复**：三个 send 都改为 1024B chunk 循环发送，ERR_MEM/WANT_WRITE 时 vTaskDelay(2ms) 重试
- **验证**：JS 72504/72504 bytes、CSS 1657/1657 bytes 完整；verify_all.py 6/6 PASS；浏览器刷新 UI 正常

## 第十五波：Vue 前端重构（Ant Design 风格后台）
- 用户要求：参考 AntD 风格后台，标题"嵌入式Web管理系统"，左侧菜单+右侧内容，多状态图标；先 Vue 验证再移植嵌入式
- 选型：**手写 AntD 风格零依赖**（体积可控，不引 ant-design-vue），4 菜单：系统概览/硬件监控/网络状态/参数设置
- 新增组件：`src/components/SvgIcon.vue`(内联SVG图标集)、`StatusDot.vue`(状态圆点)、`StatCard.vue`(统计卡+图标)
- 重写：`App.vue`(Sider暗色+Header+Content三栏)、`DashboardView/HardwareView/NetworkView/SettingsView.vue`
- `style.css`：AntD 设计令牌（暗色侧栏 #001529 + 浅色内容 #f0f2f5），状态色绿=on/在线 红=off/离线
- 新增 `scripts/gen_web_assets.py`：动态扫描 web/dist（index.html + assets/*.css/*.js）生成资源表，不再硬编码哈希名
- 构建：Debug text=252196B / Release text=272848B（含 86775B 网页资源，Flash 余量充足）；双构零警告（仅 RWX 链接器提示）
- 验证：vite build 成功(js 80.84KB/gzip31.5KB)；dev server 9 模块全 200；ST-Link 烧录 Verified OK；verify_all.py 6/6 PASS；新 JS/CSS 完整不截断(81759/4628B)

## 第十六波：Dashboard 概览增强（状态紧凑 + LED/BEEP 控制前置）
- 用户反馈：① 统计卡片"状态分两行"视觉分散 ② 网页需增加 LED/BEEP 控制按键
- **视觉优化**：`.stat-card` 由横排"图标+标题/值"改为 AntD Statistic 两段式（顶部图标+标题、底部大值+动作区），卡片高度自适应不再膨胀；min-height 96px
- **StatCard.vue**：新增可选 `#action` 插槽（LED/BEEP 卡内嵌 Switch 开关）
- **DashboardView.vue**：LED/BEEP 卡直接嵌入 Switch 切换（`POST /api/control`，busy 锁防双击）；
  运行状态明细卡 LED/蜂鸣器行也改为「状态文本 + Switch」；复用 api.js 的 controlDevice()
- HardwareView.vue 保留原 Switch（备用入口），不重复控制
- **顺带修两个后端 Bug（浏览器实测才暴露）**：
  1. POST 返回 `Connection reset`：http_server.c 在 `netconn_close` 后紧接 `netconn_delete`
     立即销毁 pcb → LwIP 发 RST。改为 close 后 recv-drain 直到对端关闭或 2s 超时再 delete
  2. 双 key body `{"led":0,"beep":0}` 时 beep 不生效：原 `json_get_int` 用裸 `strstr`+`strchr`
     找值，对第二个 key 解析错位。改为 key 必须是完整 JSON token（前导 `"`/`{`/`,` + 后随 `"`）
     再找冒号后数字；加大小写无关查找 stristr（newlib-nano 无 strcasestr）
- 验证：Debug text=254676B / Release text=275408B（含 web_assets 88915B）；双构零警告（仅 RWX）
  ST-Link 烧录 Verified OK；verify_all.py 6/6 PASS；
  单 key 与双 key POST /api/control 均验证 led/beep 0↔1 正确回读；JS 83574B/CSS 4953B 完整
- 教训：浏览器/requests 对 RST 比 curl 严格，POST 控制类 API 必须用浏览器或 requests 实测，curl 易漏判

## 网页控制 LED 改用非心跳灯 (18:48)
- 原网页 /api/control 控制 LED0/PB1，但 PB1 是心跳灯（led_task 500ms 闪烁），会冲突
- 改为控制 **LED1/PB0（非心跳）**：web_serve.c:180 BSP_LED_On/Off(0)→(1)
- main.c:60 启动 `BSP_LED_On(1)` 常亮 → 改 `BSP_LED_Off(1)`，PB0 默认熄灭并完全交还网页控制
- PB1 心跳保持不动（led_task）；PB0 启动失败路径 Error_Handler 的 BSP_LED_Toggle(1) 保留
- README 第132行补充"网页控制 LED=PB0 非心跳，PB1=心跳不受网页控"
- 验证：双构零警告(Debug 254676B/Release 275408B)；烧录 OK；POST led 0↔1 回读正确；
  PB1 心跳不受影响

## 硬件信息周期采集 + 共享结构体 (19:13)
用户要求：硬件读取独立线程周期更新(200ms)、结构体存储、web 临界区读取、为多接口(telnet/snmp)准备
- 拆两个结构体：`hwinfo_static_t`(MCU/clock/ip/mask/gw/mac/freertos_tasks，init 后不变)
  + `hwinfo_dynamic_t`(AP3216C lux/ps/ir, MPU9250 9轴, sensor_valid, led_on/beep_on, updated_ms)
- 并发模型：采集线程填局部 → 临界区内整体 memcpy 进 g_dyn（原子发布）；读取接口
  hwinfo_static_copy/dynamic_copy 临界区内整体 memcpy 出；控制 hwinfo_set_led/beep
  临界区改单字段 + 临界区外驱动硬件。保证单字段赋值是原子操作，读者永不见半更新
- 关键坑：file(GLOB app/*.c) 在 CMake 配置时展开，新建 hwinfo.c 后必须重跑 `cmake ..`
  重新 GLOB（ninja 增量不会自动重新扫描），否则 undefined reference hwinfo_*
  注意：本工程无 CMakePresets.json，`cmake --preset` 会静默失败，真构建目录是 build/ (build-release/)
- hwinfo.c 需 #include <string.h>（strncpy/memset）；采集线程保留 g_dyn.led_on/beep_on
  不被传感器刷新冲掉（拷贝前从全局回填控制态）
- main.c hwinfo_init() 在 https_server_init 后、vTaskStartScheduler 前调用（与现有 web server 同模式）
- 验证：Debug 255160B/Release 275988B 双构零警告；烧录 OK；/api/hardware 返回嵌套 JSON
  (ap3216c{lux,ps,ir,valid}, mpu9250 9轴, led/beep, tasks=4)；lux 在 250ms 间隔取样持续变化
  → 证明 200ms 后台刷新且 web 访问不阻塞；verify_all.py 6/6 PASS

## 串口 Shell 实现 (19:42)
用户要求：调试串口升级为 shell，中断收发+缓冲+回显+回车解析，历史缓存3条指令，
独立解析线程(后续telnet复用)，命令 hw/dev/net/version/beep on|off/led on|off + history。
- 新增 app/shell.c/h：shell_task(prio2) 从 RX 环形缓冲取字节回显、BS退格、\r\n触发整行解析；
  shell_exec(line,out) 解析+调 hwinfo_*/hwinfo_set_*，out 回调解耦(UART用 uart_out)；
  历史 g_history[3][128] 环形，history 命令查看
- bsp_uart.c 改为中断收发：RX(256)/TX(512) 环形缓冲；uart_puts/uart_write 互斥(g_tx_mutex)
  保护入队触发 TXE 中断；USART1_IRQHandler 收/发；删原阻塞 _write
- syscalls.c _write 改调 uart_puts（全工程 printf 走新通道）
- main.c BSP_UART_Init 后 shell_init()
- 关键坑1：RXNEIE 用 `huart1.Instance->CR1 |= USART_CR1_RXNEIE`(0x20) 寄存器直接置位才生效
  （SET_BIT/__HAL_UART_ENABLE_IT 在本HAL版本 CR1 位未生效，gdb读 CR1 仍 0x200c）
- 关键坑2：调度器启动前 printf 不能走互斥中断发送，uart_write 判断
  xTaskGetSchedulerState()!=RUNNING 时回退 HAL_UART_Transmit 阻塞，否则 xSemaphoreTake
  (portMAX_DELAY) 死锁/触发 heap_4.c:269 configASSERT
- 验证：双构零警告(Debug 259088B/Release 280072B)；烧录 OK；gdb仿真确认 CR1=0x202c(RXNEIE
  使能)、shell_exec 各命令返回0
- 物理串口交互未验证：当前PC仅COM3(CP210x)，gdb写USART1 DR+resume 后COM3无字节→
  CP210x 与 USART1(PA9/PA10) 物理连线当前未接通。用户确认 CP210x 接 USART1，重插线后可测

## 串口指令压测 (20:05)
用户要求"直接进行串口指令压测"。物理串口线仍未通（WMI/CIM 均未枚举到 CP210x），故做
**PC 侧对真实 app/shell.c 解析逻辑压测**：
- 新增 `tests/shell_stress/`：inc/ 桩头(bsp_uart/hwinfo/netcfg/led/pcf/FreeRTOS) + shell_stub.c
  桩实现 + stress_shell.py(gcc 编 DLL + ctypes 驱动 shell_exec/shell_feed_line)
- **重构**：从 shell_task 抽出 `shell_feed_line(line)` = history_push + shell_exec，回车路径与
  未来 telnet 共用；shell.h 导出。行为不变
- 覆盖：① 正常指令功能正确性 ② 边界(空行/前后空格/Tab) ③ 超长行127~500B安全截断
  ④ 异常(未知命令rc=-1+Unknown / 缺参错参rc=0+Usage) ⑤ history环形缓冲(真实回车路径,
  保3条/挤最旧/空行不进) ⑥ 随机10000条混合
- **结果 41 PASS / 0 FAIL**，随机负载 ≈81万 cmd/s，无崩溃/越界
- 双构复验零警告：Debug text=259292B/FLASH24.73%、Release text=280304B/FLASH26.73%
- README 已加"串口指令压测"小节；物理真机压测仍待用户重插 CP210x 连线

## 第十七波：heap_4.c:269 断言根因 = SDRAM 晚于 FreeRTOS 对象创建 (20:30)
- 用户报 `heap_4.c:269 heapSUBTRACT_WILL_UNDERFLOW` 断言，且"增加 shell 之后运行异常"
- **根因（已确认）**：本工程 FreeRTOS heap(ucHeap)+LwIP ram_heap+mbedTLS 池全在 SDRAM。
  任何 xTaskCreate/xSemaphoreCreate* 都从 ucHeap 分配。原 main.c 在 `bsp_sdram_init()`
  **之前**就调 BSP_UART_Init()(建 g_tx_mutex) 与 shell_init()(xTaskCreate shell 任务)，
  把控制块写进未初始化的 SDRAM → 空闲链表元数据损坏 → 269 下溢断言。加 shell 首次在
  SDRAM 就绪前建任务才踩响。512KB heap 本身足够，不是容量问题
- **用户决策**：不拆 BSP_UART_Init/shell_init（拆分不利维护），改为**把 bsp_sdram_init()
  提前到 HAL_Init+时钟之后、所有硬件初始化之前**（最干净，后续任何 init 建 FreeRTOS 对象都安全）
- **改动**：
  1. main.c：bsp_sdram_init() 从 line82 前移到 SystemClock_Config() 之后（最早）；删原处重复调用
  2. 回退误加的 4KB heap_guard 隔离带（错误方向，且空 NOLOAD 段被 ld 丢弃无效）：
     - sdram_heap.c 删 heap_guard[4K]
     - 链接脚本 .ld 删 .sdram_heap_guard 段（map 确认 lwip_pool_end 紧邻 freertos_heap_start）
  3. bsp_uart.c 保持原样（mutex 在 BSP_UART_Init 内创建，现因 SDRAM 已先起而安全）
- **约束（后续实现必须遵守）**：新增任何在 main() 创建 FreeRTOS 对象的初始化(BSP/驱动/中间件)，
  调用点必须在 bsp_sdram_init() 之后；不要为规避而拆分对象创建逻辑
- 验证：双构零警告(Debug 259256B/FLASH24.72%、Release 280252B/FLASH26.73%)；SDRAM 总占 688160B
  ucHeap 0xC0020020~0xC00A0020(512KB) 布局确认；RWX 为唯一链接器提示（历史良性）
- README：架构图改"SDRAM 最先"、踩坑 12 增补、Shell 关键坑第2条修正根因说明
- **待真机验证**：CP210x 连线未通，断言是否消失需烧录后实际运行确认（代码时序已根治）

## 第十八波：PRINT_LOG 日志改造 + 启动死机修复 (20:45)
- 用户报：`BSP_UART_Init()` 后任务执行期**死机**，定位为 printf 底层走 mutex 导致异常
- **根因**：应用层用 `printf` → newlib `_write` → `uart_write`，在任务上下文与
  `BSP_UART_Init` 创建的 `g_tx_mutex` 交互异常（调度器运行态下某路径触发死锁/异常）
- **用户要求**：任务中不要使用 printf，改用 uart_puts 或自建 `printf_log`（完整保护 + 打印）
- **实现**（app/log.h / app/log.c）：
  1. `PRINT_LOG_ENABLE` 编译期总开关：置 0 时 `PRINT_LOG(...)` 宏展开为空、
     `printf_log()`/`vprintf_log()` 直接 return → 一键关闭全部打印，零运行时开销
  2. `printf_log(fmt,...)` / `vprintf_log(fmt,ap)`：栈上 vsnprintf(160B) 格式化 →
     `uart_write()`；uart_write 按调度器状态分流（RUNNING→互斥 ring 入队；未运行→
     HAL_UART_Transmit 阻塞），**RTOS 已启动与未启动两种状态都安全**；不调 newlib printf
  3. `PRINT_LOG(fmt,...)` 宏：ENABLE=1 展开 printf_log，ENABLE=0 展开 ((void)0)
- **替换范围**（应用层全面弃 printf）：
  main.c(6处)、freertos_hooks.c(3处 assert/overflow/malloc)、http_server.c(5处)、
  https_server.c(13处)、ethernetif.c(2处)、bsp_sdram.c(4处) 全改 PRINT_LOG；
  syscalls.c `_write` 仍直接 uart_write（不经 printf，作漏网 stdio 兜底安全通道）
- **验证**：
  - 双构零警告（仅 RWX）：Debug text=259276B / FLASH 24.73%；Release text=280256B / FLASH 26.73%
  - 烧录 Verified OK
  - **gdb 仿真（openocd reset run 4s 读 uxCurrentNumberOfTasks=0x2000f004）= 9** →
    调度器正常启动、所有任务创建成功、**未死机/未触发 heap_4 断言**，PRINT_LOG 双状态路径生效
- README：Shell 关键坑区补充 PRINT_LOG 改造；构建验证过程补第6条

## 第十九波：uart 初始化前禁打印（移除 sdram/uart 内打印）
- **约束**：uart 真正初始化（`BSP_UART_Init()`）之前，不得有任何打印调用。
- **根因**：`bsp_sdram_init()` 在 `BSP_UART_Init()` 之前执行，但 bsp_sdram.c 内原本有 3 处
  PRINT_LOG（FMC init 失败 / init sequence 失败 / OK 成功横幅）+ sdram_memory_test 失败打印，
  这些发生在 huart1 未初始化时，会走 `uart_write` 的 polled 回退（发无意义字节甚至异常）。
- **改动**：`bsp/bsp_sdram.c` 删除全部 4 处 PRINT_LOG 及 `#include "log.h"`、`<stdio.h>`
  （失败仅返回 -1，交由 main.c 的 `Error_Handler()` 用 LED 指示；成功无横幅）。
  `bsp/bsp_uart.c` 本就无打印，uart 内部保持干净。
- **保留**：main.c:70 起（ETH PHY 释放、传感器、scheduler starting 等）、ethernetif/http/https/
  freertos_hooks 的 PRINT_LOG 均在 `BSP_UART_Init()` 之后，合法保留。
- **验证**：Debug / Release 双构零警告（仅 RWX 良性提示）；
  Debug text=258864B / FLASH 24.71%，Release text=279844B / FLASH 26.71%。

## 第二十波：修复 USART1 中断未真正挂载
- **问题**：`startup_stm32f429xx.s` 里 `USART1_IRQHandler` 仅是 `.weak Default_Handler`，
  工程从未定义真正的向量函数，原 `BSP_UART_IRQHandler()` 无人调用 → RX/TX 中断全进
  Default_Handler（空转/死循环），串口收发实际不工作。
- **修复**：`bsp/bsp_uart.c` 新增真实 `USART1_IRQHandler(void)`，内部 `BSP_UART_IRQHandler()`；
  保留既有 RX/TX 处理逻辑不动。`bsp/bsp_uart.h` 注释改为"USART1 ISR 转发调用"。
- **验证**：map 确认 `USART1_IRQHandler` 绑定到 bsp_uart.c.obj 真实实现
  (Debug 0x080045d8，紧邻 BSP_UART_IRQHandler 0x0800453c)，脱离了 Default_Handler(0x08004634)；
  Debug/Release 双构零警告（仅 RWX）：Debug text=259024B/FLASH24.72%，Release text=279988B/FLASH26.72%。

## 第二十一波：RX 改 xQueue + TX 临界区 + 调度器前丢弃 RX
- **RX 接收机制**：`bsp/bsp_uart.c` 删除 `g_rx_buf/g_rx_head/g_rx_tail/rx_free` 环形队列，
  改为 `static QueueHandle_t s_rx_queue`，`BSP_UART_Init()` 中 `xQueueCreate(UART_RX_BUF_SIZE,
  sizeof(uint8_t))`（失败 Error_Handler）。ISR 用 `xQueueSendFromISR` + `portYIELD_FROM_ISR`
  投递字节；`uart_getc` 改 `xQueueReceive(.,0)` 非阻塞。彻底消除 ISR/任务共享 head/tail 错位丢字节。
- **TX 保护**：`uart_write` 入队段包 `taskENTER_CRITICAL/EXIT_CRITICAL`，保护 g_tx_head/tail、
  g_tx_busy、CR1.TXEIE 不被 USART1 ISR 并发改写；g_tx_head/tail/busy 标 volatile。
- **调度器前丢弃 RX**：ISR RX 分支增加 `xTaskGetSchedulerState()==RUNNING` 判断，未启动
  时仅读 DR 清 RXNE 后丢弃，不碰队列（xQueueSendFromISR 在调度器前不安全）。
- **shell.c**：`SHELL_LINE_MAX` 128→64；BS(`\b`/0x7F) 回显 `\b \b` 逻辑保留（已正确）。
- **验证**：Debug/Release 双构零警告（仅 RWX）：
  Debug text=259072B/FLASH24.72%，Release text=280036B/FLASH26.72%。

## 第二十二波：下载+调试真机验证 (xQueue 版)
- **PC 逻辑压测**：`tests/shell_stress/stress_shell.py` 复用（编真实 shell.c），
  41 PASS / 0 FAIL（含 10000 条随机指令、history 环形、超长行截断不崩）。
- **真机下载调试**（openocd + arm-none-eabi-gdb）：
  - 烧录 Verified OK（33 KB/sec，259256 B）
  - `uxCurrentNumberOfTasks = 9` → 调度器启动、9 任务含 shell_task 创建成功、无死机/heap 断言
  - `s_rx_queue` 结构合法：handle=0xC0020080（SDRAM/ucHeap）、uxLength=256、uxMessagesWaiting=0
  - 符号在位：`USART1_IRQHandler`@0x8004600（脱离 Default_Handler）、`BSP_UART_IRQHandler`@0x800454c、`shell_task`@0x800287c
  - USART1 CR1=0x202C（UE+TE+RE+RXNEIE 全开）、NVIC ISER1 bit5=1（USART1 中断使能）
- **结论**：RX 队列路径（ISR xQueueSendFromISR → uart_getc xQueueReceive → shell_task）、
  TX 临界区路径、调度器前丢弃逻辑均已在位；物理 RX 字节需用户插 CP210x↔USART1(PA9/PA10) 真机验证。

## 第二十三波：修复输入字节翻倍 (he -> hhelphehe)
- **现象**：用户实际输入 `he`，MCU 回显/执行变成 `hhelphehe`（同一字节被重复投递）。
- **根因**：`bsp/bsp_uart.c` 的 `BSP_UART_IRQHandler` 在 **RX 分支内每字节调用一次
  `portYIELD_FROM_ISR`**。该宏仅向 NVIC INT_CTRL 的 PendSV 位做无 `dsb/isb` 屏障的 store，
  与"读 DR 清 RXNE"紧邻 → ISR 退出瞬间重踢 Pending 的 RXNE → 同字节 `xQueueSendFromISR` 两次。
- **修复**：
  1. RX 分支移除内联 `portYIELD_FROM_ISR`，读 DR 后加 `__DSB()`；
  2. yield 改为累积单个 `xWoken`，**仅在函数最末** `portYIELD_FROM_ISR(xWoken)`（RX 不需 yield，
     TX 完成解锁任务才需，故保留末位一次）；
  3. `app/shell.c` 回显单字节由 `uart_puts((char*)&c)`（依赖 strlen 易越界）改为 `uart_write(&c,1)`。
- **验证**：Debug/Release 双构零警告（仅 RWX）；Debug text=259072B/FLASH24.72%, Release text=280052B/FLASH26.73%。
  真机 COM3 串口回归 14/14 PASS，关键用例"输入 he -> MCU 收到 he(无 hhelphehe 翻倍)"通过。

## 第二十四波：MPU9250 显示空白 + 方向键丢弃
- **问题1 MPU9250 显示全空**（`MPU9250 ax/ay/az :  /  /`）：根因是 newlib-nano
  (`-specs=nano.specs`) 默认不链接浮点 printf，`%.2f` 输出被裁剪为空。
  `sensor_valid=1` 证明驱动/读取均成功，只是打印层丢数字。
  修复：CMakeLists 链接器加 `-u _printf_float`（Flash 增 ~8.6KB，余量充足）；
  Debug text 259072->267684B (FLASH 25.58%)，Release 279988->288796B (27.59%)。
- **问题2 方向键回显/存储**：shell_task 新增 ANSI escape 状态机 (esc_state 0/1/2)，
  收到 ESC(0x1B)->ESC[->final byte(A/B/C/D) 整串丢弃，不回显不存储，line buffer 不变。
- **验证**：Debug/Release 双构零警告（仅 RWX）；真机验证脚本 verify_uart_hw.py 增 2 用例
  （MPU9250 含数字 / 方向键 ESC[A/B/C/D 丢弃后仍正常解析 hw/version），待 COM3 释放后跑。
