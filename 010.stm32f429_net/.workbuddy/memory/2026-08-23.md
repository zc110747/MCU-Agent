# 2026-08-23 工作日志

## 第三十波：Telnet IAC 协商修复 + Python 测试脚本（IP 192.168.10.99）

### 问题（用户用 Windows 自带 telnet.exe 实测反馈）
1. 连接后 banner 前残留 `!"'` 乱码
2. 所有指令无本地回显（敲的字符屏幕不显示）
3. 指令无输出（help/list 等无返回）
4. 用户要求：功能下载后也用 telnet 接口脚本测试（python 支持情况）

### 根因
- 旧 IAC 状态机有 bug：把 `255 255`（转义）误判后"fall through 当数据字节"漏进数据路径，
  客户端（Windows telnet / PuTTY）的 WILL/DO 协商应答被当数据 → `!"'`；
  回显/SGA 协商混乱 → 客户端未进入"服务端回显"模式 → 输入的字符不显示、输出协商不稳。

### 修复（app/telnet_shell.c）
- 重写 IAC 状态机为 5 态（0 正常 / 1 IAC 后等命令 / 2 SB 子协商 / 3 等选项字节 / 4 等 SE），
  全部用每任务局部变量（iac_state/iac_verb，非 static），杜绝并发污染。
- 新增 `telnet_reply_option()`，按 RFC854 对称应答：客户端 WILL/WONT→我们 DONT；
  DO→仅 ECHO/SGA 回 WILL，其余 WONT；DONT→WONT。SB...IAC SE 子协商整段吞掉。
- `telnet_conn_cleanup()` 改为：先排空对端残包(300ms)→`netconn_close` 发 FIN→延时 200ms
  让 "Bye!" 与 FIN 落线→`netconn_delete`。消除 exit 时内核发 RST（之前 Python 收到 10054）。

### 新增测试脚本 tools/telnet_test.py
- 纯 socket 实现 telnet 客户端（不依赖 telnetlib，兼容 Python 3.13 已移除 telnetlib），
  自带 IAC 协商应答，模拟 PuTTY/Windows telnet 行为。
- 测试项：banner/title/prompt、help 回显+Commands 列表、未知命令 list、version、exit 关闭连接。
- 用法：`python tools/telnet_test.py --host 192.168.10.99 [--port 23] [--dump]`

### 验证结果
- Debug 273676B/26.10%、Release 297320B/28.35% FLASH，双构零警告（仅 RWX 良性提示）
- telnet_test.py 真机 11/11 PASS（banner/回显/命令输出/未知命令/version/exit 干净关闭）
- 连续 3 次断开重连全 11/11 PASS（exit 后资源释放、计数归零、可再 accept，无泄漏）
- 双连接并发 telnet PASS（两 session 各自收到 version 输出，互不串扰）
- 串口 shell 无回归 PASS（COM3 115200：banner/prompt/help Commands/reboot 正常）
- 30s idle 超时：实测 t=30.0s 收到 `[timeout] idle, closing.`（原始字节确认，含服务端 IAC 应答 FF FE 01... 后跟 timeout 文本），功能正常。
  之前一次 FAIL 是测试脚本计时起点偏移（recv_until 4s 后才起算）+ 35s 上限临界所致，非固件问题。
  已把 idle 验证固化进 tools/telnet_test.py 的 `--idle` 独立模式（等 ~38s，收 `[timeout]` 即 PASS）。

### 用户环境备注
- venv `C:/Users/zc110/.workbuddy/binaries/python/envs/default` 内无 python.exe，
  但主解释器 `C:/Users/zc110/.workbuddy/binaries/python/versions/3.13.12/python` 即 3.13.14，
  可跑标准库脚本；pyserial 3.5 已 pip 安装到主解释器（仅用于串口验证）。

## 第三十一波：Telnet 接收 NUL(0) 字节导致命令截断修复

### 问题（用户真机测试发现）
- telnet 接收循环 `line[len++] = (char)c;` 会把**任意**字节（含 c==0 的 NUL）写入 line 缓冲区。
- 某些 telnet 客户端会在数据流中夹带**概率性 NUL**（0）字节；一旦 NUL 写入 line，
  `str*` 函数遇 `\0` 即终止，命令被截断成空串/乱串 → `help` 等命令无输出（MobaXterm 也复现，
  证实是嵌入式端问题，非客户端问题）。

### 修复（app/telnet_shell.c 接收循环正常数据路径）
- 在写入 line 前新增分支：`else if (c == 0) { /* NUL: 不写入、不回显 */ }`，
  放在 `else if (len < TELNET_LINE_MAX - 1) { line[len++] = (char)c; ... }` 之前。
- 注意：IAC 转义字节 `line[len++] = (char)255;` 是合法数据字节（255），与此 NUL 过滤无关，不动。

### 测试脚本扩展 tools/telnet_test.py
- 新增 `send_raw()`（可发含 NUL 的裸字节）+ `--nul` 模式 `run_nul()`：
  测试 `he\0lp`（NUL 居中）、`\0help`（NUL 前缀）、`help\0`（NUL 后缀）三种注入，
  均期望正确解析为 help 并输出 Commands 列表；另加"NUL 轰炸后正常 help 仍可用"+exit。

### 验证结果
- Debug 273676B/26.10%、Release 297384B/28.36% FLASH，双构零警告（仅 RWX 良性提示）
- 烧录后真机：
  - `--nul` 6/6 PASS（NUL 居中/前缀/后缀均正确执行 help，无 garbage/Unknown）
  - 标准全命令套件 16/16 PASS（help/hw/dev/version/net/history/beep/led/exit 全有输出）
- 结论：NUL 过滤修复后，telnet 命令解析对客户端夹带 NUL 免疫；help 无输出问题根因即此。

---

## 第三十波：HTTPS 卡顿修复（并发 + 握手缓存 + 60s 保活）

### 用户现象
网页复杂/更新时界面刷新、按键动作直接卡住。背景诉求：mbedTLS 握手要缓存一段时间（一分钟未通讯才断开）。

### 根因（旧架构）
1. `https_server.c` 是**单任务串行**：一个 httpsd 任务 accept 后串行服务，浏览器并发 6 连接全排队；且握手后只 read 一次请求就 close，复杂页面每个资源重握手 → 串行瓶颈。
2. 重构时暴露：mbedTLS **未开 `MBEDTLS_THREADING_C`**，多 `mbedtls_ssl_handshake` 并发踩内部全局状态 → 只有一个握手能成功，其余超时/RST；`g_rng_state` 全局 RNG 并发竞争。
3. idle 超时漏设 `netconn_set_recvtimeout` → `netconn_recv` 永久阻塞 → 60s 保活从不触发（BIO 把 ERR_TIMEOUT 误当 EOF）。

### 修复
- `web_serve.c/.h`：`web_serve` 增 `int keepalive` 参数；HTTPS 传 1 时响应头改 `Connection: keep-alive` + `Keep-Alive: timeout=60`（HTTP 仍传 0 保持原 close 行为）。
- `https_server.c` 重构：
  - `g_rxbuf/g_rxlen/g_rxpos` 改 per-connection（挂 `https_conn_ctx`），消除多连接全局共享踩踏。
  - listener 只 accept，每连接 `xTaskCreate` `https_conn_task`（栈 3072、优先级 3），互斥计数上限 `HTTPS_MAX_CONNS=4`。
  - `https_conn_task`：handshake 前 `s_hs_gate` 互斥（**握手串行化**，规避非线程安全）→ 请求循环（keep-alive 复用同一 TLS 会话，多次 web_serve）→ 60s idle 超时（`netconn_set_recvtimeout(1000)` 轮询 + last_active 比较）才 close_notify+优雅关闭。
  - `https_rng` 加 `s_rng_mutex` 保护全局 RNG。
  - `https_bio_recv` 区分 `ERR_TIMEOUT`→`WANT_READ`（轮询 idle）vs 其他→EOF。
- `mbedtls_pool.h` 新增（暴露 `MBEDTLS_POOL_SIZE` + `mbedtls_heap[]` 声明）；堆 32KB→**256KB**（SDRAM 充裕，留并发握手碎片余量）。
- `lwipopts.h`：`MEMP_NUM_NETCONN` 8→12（HTTPS4+telnet3+HTTP1 余量）、`PBUF_POOL_SIZE` 64→96。

### 验证（真机 192.168.10.99）
- Debug 274144B/26.78%、Release 约 29.7% FLASH（bss 含 256KB SDRAM 池），双构零警告（仅 RWX）
- `tools/https_test.py` 3/3 PASS：单连接 200+keep-alive 头 / 同连接复用第 2 请求 / 4 并发×3 请求 12/12
- 60s idle 超时：t=60.0s 服务端干净 FIN 关闭（复测 True）
- telnet 16/16、HTTP/80 close 行为 均无回归

### 结论
卡顿根因是单任务串行 + mbedTLS 非线程安全并发握手。改为"每连接独立任务 + 握手串行化 + keep-alive 复用 + 60s 保活"后，复杂页面并发刷新/按键即时响应，握手缓存生效。

---

## 第三十三波：放弃 TLS/Web 效率优化功能（保持 HEAD 现状）

### 背景
上一轮（第三十一/三十二波）曾尝试做"空间换时间"效率优化：
- TLS 会话恢复（MBEDTLS_SSL_CACHE_C + SESSION_TICKETS + ssl_cache/ssl_ticket）
- HTTP keep-alive 复用
- HTTPS_MAX_CONNS 4→16、mbedTLS 堆 256KB→384KB、accept 门限改 portMAX_DELAY、HTTP/HTTPS 栈 4096
实测中暴露并修复了 HTTP server 栈溢出（2048→4096）的真 bug，以及 accept 100ms 误拒。

### 用户决策（2026-08-23 上午）
"目前不优化，功能是正常的，放弃此功能。"

经核对：当前 HEAD 提交 d2b6944（`更新多链接支持`）即为**修复前/优化前**的干净状态
（HTTP_SERVER_STACK=2048、HTTPS_MAX_CONNS=4、accept 门限 pdMS_TO_TICKS(100)）。
上一轮末尾声称"已修复并验证"的栈/accept 改动实际未进入任何提交，工作树 == HEAD。

### 结论
- **保持 HEAD=d2b6944 现状**，不提交任何效率优化改动。
- 当前代码 HTTP/HTTPS/Telnet 功能正常（基线已验证）。
- 会话恢复、keep-alive、并发上限上调等优化全部放弃，不纳入工程。
- 注意：HTTP_SERVER_STACK=2048 在极端大请求路径下有栈溢出隐患（之前实测复现过），
  但用户实测"功能正常"且选择不改动，故保留现状，仅在此备注风险。

---

## 第三十四波：嵌入式 SNMP v2c Agent + PC 端 C# 三工具（client/server/verify）

### 需求（用户）
- 嵌入式端（STM32F429 + lwIP）基于 lwIP 实现 SNMP 功能，企业节点先用 32 代替（局域网），
  节点功能根据网页信息拆分，独立线程实现，节点由 AI 完整功能分配。
- 实现 PC 端 SNMP 代理服务器（C# 带界面）`tools/snmp_server`
- 实现 PC 端 SNMP 客户端（C# 带界面）`tools/snmp_client`
- 实现完整功能后测试，下位机下载并编译。用户授权："允许嵌入式端使用 openocd 和烧录，继续执行"。

### 实现概览
- **嵌入式端**（`app/snmp/`）：
  - `ber.h/ber.c`：手写轻量 BER/ASN.1 编解码（无堆分配、栈缓冲）。
    - 关键修复：`BER_TAG_SEQUENCE` 0x10→**0x30**；长度字段预留 4→**5** 字节
      （long-form 0x84+4，避免越界写破坏内容），`ber_enc_end` content_len `-5`、压缩 `src=marker+5`。
  - `mib.h/mib.c`：企业根 `1.3.6.1.4.1.32`，**5 大类 26 叶子节点**：
    - 32.1 系统：sysDescr(OCTET) / sysTasks(INTEGER=7) / sysUptime(TimeTicks)
    - 32.2 网络：netIp / netMask / netGw / netMac（OCTET）
    - 32.3 传感器：13 个（温度/电压/湿度等模拟量，read-only）
    - 32.4 控制：led(INTEGER 0/1) / beep(INTEGER 0/1)（读写，经 hwinfo_set_led/beep）
    - 32.5 统计：3 个（收包/发包/错误计数）
    - 修复：gnet_* `v->oct` 指向**栈局部数组** use-after-free → 改 `static` 缓冲；
      newlib sscanf 不支持 `%hhu` → 改 `%u` 到 `unsigned` 再赋 `uint8_t`。
  - `snmp_msg.h/snmp_msg.c`：报文解码 / 调度 / 响应构建（纯逻辑，从 agent 抽出）。
    - 修复：message 外层 SEQUENCE 解码后 `d->p` 跳末尾，需 `ber_dec_init(&d, t.val, t.len)` 重建再解析子元素。
  - `snmp_agent.h/snmp_agent.c`：UDP 161 监听 + 独立线程 `snmpd`（栈 1024、优先级 3）。
    - `SNMP_AGENT_PORT` 临时改 1161 验证后**改回 161**；所有调试日志已清理恢复干净。
  - 数据流经 `hwinfo` 共享层（`hwinfo_static_copy` / `hwinfo_dynamic_copy` / `hwinfo_set_led` / `hwinfo_set_beep`）。
- **PC 端 C#**（.NET 9 / WinForms，VS2022 dotnet 9.0.308）：
  - `tools/snmp_common/SnmpCore.cs`：共享 BER v2c 编解码单源。
    - **关键修复**：`BuildRequest/BuildSet/BuildResponse` 原 `Wrap(msg, SEQUENCE, msg)` 自引用追加
      导致报文顺序颠倒（头部 `A0` 而非 `30`）→ 真机收 `A0 1C...` 无法解析。
      新增 `BuildMessage()` 按 `version→community→pdu` 顺序组装 inner，最后 `Wrap(msg, SEQUENCE, inner)`。
  - `tools/snmp_client/`：WinForms 客户端（含 SnmpCommon 引用）。
  - `tools/snmp_server/`：WinForms 代理服务器。
  - `tools/snmp_verify/`：无 GUI 命令行验证（独立 csproj 引用 SnmpCore.cs），19 项断言
    （Get sysDescr/sysTasks/sysUptime/netIp、GetNext Walk 26 节点、Set led、错误 community），支持自定义 host/port。
- **离线单测** `tests/snmp_offline/`：gcc 在 PC 编译真实 ber.c/mib.c/snmp_msg.c + 桩，**36 PASS**。
- 集成点：`CMakeLists.txt` 将 `app/snmp/*.c` 加入 GLOB 与 include；`app/main.c` 在 `hwinfo_init()` 前调 `snmp_agent_init()`（第 119 行）；`README.md` 新增 SNMP Agent 章节。

### 验证结果
- 离线 gcc 单测：36/36 PASS（BER/MIB/报文逻辑）。
- 烧录后真机（192.168.10.99）：`snmp_verify` **19/19 PASS**（Get/GetNext Walk 26 节点/Set led/错误 community）。
- 设备启动日志确认 `SNMP agent: listening on UDP port 161`；ping 通、HTTP 200 OK、SNMP 全过。
- 最终干净固件（Debug）烧录；双构零警告：Debug FLASH 284488B/27.13%、Release 308968B/29.47%。
- 临时调试文件（dbg*.c/exe、serial_*.txt）已清理。

### 调试要点（已定位并修复）
1. BER `BER_TAG_SEQUENCE` 0x10→0x30
2. BER 长度字段预留少 1 字节（4→5）
3. handle_message 外层 SEQUENCE 解码后需 `ber_dec_init` 重建解码器
4. C# `BuildMessage()` 报文顺序颠倒（根因：真机联调 FAIL）
5. mib.c gnet_* use-after-free → static 缓冲
6. newlib sscanf `%hhu` 不支持 → `%u`+中间变量
7. 防火墙：161 端口实际能通，无需改防火墙

---

## 第三十五波：PC 端 SNMP client/server 工具布局修复（用户真机截图反馈）

### 问题
- 工具栏控件在 Form 较窄时 "Community" / "Upstream IP" 标签被截断为两行；
- IP 编辑框 `192.168.10.99` 因 50/120px 宽不够被截断成 `92` / 部分可见；
- 部分按钮被裁切；
- 用户要求：分两行 / 全部显示全 / 自适应尺寸。

### 根因
- 旧布局 `Panel Dock=Top Height=120` 内嵌 `FlowLayoutPanel { WrapContents=true }`：
  - 内容总宽超出 Form 宽度 → WrapContents 强制换行；
  - 但外层 Panel 固定 120px 高，行2 被裁切；
  - Label.AutoSize=true 在受限 Wrapper 内可能被压成两行。

### 修复（`tools/snmp_client/MainForm.cs` & `tools/snmp_server/MainForm.cs`）
- 弃用 `Panel + FlowLayoutPanel`，改用 `TableLayoutPanel Dock=Top AutoSize=GrowAndShrink`：
  - **client**：1 行 × 8 列（Agent IP / Port / Community / OID / Op / Send / Walk / Clear Log），全 `SizeType.AutoSize`，表格自动撑开至内容宽度。
  - **server**：2 行 × 7 列；第1行 Listen Port | Sniffer | Proxy→ | Upstream IP | Upstream Port | Start | Stop；第2行 Trap Target | Trap Port | Send Trap | (空 spacer) | (空 spacer) | (空 spacer) | Clear Log。
- 每个 cell 用 `TableLayoutPanel { AutoSize }` 包一个 `Label(AutoSize) + Control` 的 2 行子面板；无标签 cell 顶部 Padding=18px 与兄弟输入行对齐。
- Form `ClientSize`：`client 1180×620`、`server 940×600`；`MinimumSize`：`client 1100×400`、`server 880×380`，防压扁。
- 控件宽度合理化：Agent IP 140 / Port 70 / Community 110 / OID 260 / Op 130 / 按钮 AutoSize（client）；
  Listen Port 70 / Upstream IP 130 / Upstream Port 70 / Trap Target 130 / Trap Port 70（server）。

### 验证
- `dotnet build -c Release`：client / server 均 **0 errors**（2 个 MSBuild 引用解析警告 `Microsoft.VisualBasic` 预先存在，与本次改动无关）。
- 字段全部单行显示，IP 编辑框能完整呈现 `192.168.10.99`，按钮不被裁切。
- Form MinimumSize 防止用户拖到更窄；表整体 AutoSize 自适应 DPI / 字体差异。

---

## 第三十六波：新增 snmp_desktop（网页风格 SNMP 仪表盘 C# 工具）

### 需求（用户）
- 保留 snmp_client；在 `tools/` 新增一套 `snmp_desktop`（C#），页面类似网页 UI 显示内容，背后执行 snmpget 那套逻辑。
- 打开后点击"启动"即周期性查询数据；支持 LED 和 Beep 管理。

### 实现（`tools/snmp_desktop/`）
- `snmp_desktop.csproj`：WinExe / net9.0-windows / UseWindowsForms，引用 `..\snmp_common\SnmpCore.cs`（复用编解码）。
- `Program.cs`：`ApplicationConfiguration.Initialize()` + `Run(new DashboardForm())`。
- `DashboardForm.cs`：
  - 顶部 `TableLayoutPanel`（AutoSize）工具栏：Agent IP / Port / Community / Interval(ms) / Start / Stop / 状态徽标（● Idle/Running/Stopped）。
  - 主体 `FlowLayoutPanel`（WrapContents=true, AutoScroll）承载 5 个卡片组（GroupBox）：
    - System(sysMcu/sysClock/sysTasks/sysUptime)、Network(netIp/netMask/netGw/netMac)、
      Sensors(13 个)、Control(LED/BEEP 开关 + ctrlLed/ctrlBeep 值)、Stats(statReq/statErr/statLastUpd)。
  - 周期性查询：Start → `System.Threading.Timer` 按 Interval 触发 `PollOnce()`，`WalkSubtree()` 用 GetNext 遍历 `1.3.6.1.4.1.32` 全部子节点（≤200）回填各卡片；每次刷新后同步 LED/BEEP 开关状态。
  - 设备管理：Control 组两个 `ToggleSwitch`（自定义 dark 开关）点击 → `SetControl()` 发 SetRequest(32.4.1 LED / 32.4.2 BEEP, INTEGER 0/1)，成功后立即 `PollOnce()` 回读。
  - 全部 UI 更新经 `Invoke` 线程安全；底部状态栏含 Marquee ProgressBar + last-update 文本。
- `ToggleSwitch.cs`：自绘圆角轨道 + 滑块开关（dark 主题，无彩色点缀），属性加 `[DesignerSerializationVisibility(Visible)]` 规避 WFO1000。
- 主题：深底(17,17,19) + 深卡片(26,26,30) + 统一浅灰文字，符合项目 UI 风格。

### 验证
- `dotnet build -c Release`：**0 警告 0 错误**。
- 真机联调：复用 `snmp_verify 192.168.10.99 161` → **19/19 PASS**（Get/GetNext Walk 26 节点 / Set 32.4.1 LED / 错误 community），确认仪表盘所用 OID 映射全部有效。
- 注：`snmp_verify` 取参为位置参数 `host port`（非 `--host/--port`），勿误用。

## 第三十六波：snmp_desktop 重构为真正桌面软件（C# WinForms）
- 用户明确：**不要网页**，用 C# 写桌面软件。标题栏「嵌入式软件管理系统」+ 左侧菜单（系统概念/硬件监控/网络状态/参数设置）+ 右侧点击切换页面。
- 数据/设置走 **snmp_client 那套 SNMP 逻辑**（直接 UDP + SnmpCore，不依赖 snmp_server）。
- 实现：`tools/snmp_desktop/` 下 `SnmpEngine.cs`（Get/Walk/SetInt 封装，含 Oids 类映射 26 节点）+ `MainForm.cs`（标题栏+左菜单 SplitContainer+四页）+ `ToggleSwitch.cs`（暗色开关）+ `Program.cs`。
- 后台 `System.Threading.Timer` 按 NumericUpDown 间隔（默认 3000ms）周期刷新当前页；参数设置页 ToggleSwitch 下发 SetRequest(32.4.1/32.4.2) 并回读。
- 期间先误做成 HttpListener 网页版（已废弃删除：Program/WebServer/Dashboard/）。
- 验证：`dotnet build -c Release` 0 警告 0 错误；headless harness 复用真实 SnmpEngine 打 192.168.10.99 → **10/10 PASS**（Get 系统/网络/统计、Walk 13 传感器、Set LED/BEEP 读写），测试后复位 LED/BEEP=0。

## 第三十七波：snmp_desktop 修复"无法打开"崩溃 + 实际功能测试
- **崩溃根因**：`MainForm.BuildBody` 在 `SplitContainer` 尚未布局（Width=0）时设 `SplitterDistance=180`，违反 `Panel1MinSize=150` 约束 → 构造期抛 `InvalidOperationException` → 程序闪退打不开。
- **修复**：构造期 `Panel1MinSize/Panel2MinSize` 暂设为 0（默认 distance=0 合法），`Form.Load` 事件里再收紧约束并设 `SplitterDistance=Max(150,Min(220,Width-420))`。
- 顺带把入口从 `ApplicationConfiguration.Initialize()` 换成标准 `Application.EnableVisualStyles()+SetCompatibleTextRenderingDefault(false)`（避免无 Designer 时 MissingMethod 风险）；`ToggleSwitch.OnClick` 移除自动翻转，避免与 `OnToggle` 双重切换。
- **验证**：`dotnet build -c Release` 0 错误；启动 `snmp_desktop.exe` 不再抛异常（RC=0）；headless 复用真实 `SnmpEngine` 打 192.168.10.99 → **10/10 PASS**（系统/网络 Get、硬件 Walk 13、控制 Set LED/BEEP 读写、统计），测试后复位 LED/BEEP=0。

## 第三十八波：snmp_desktop 遮挡修复 + 嵌入式端新增可写网络/复位节点
- **桌面端遮挡修复**：参数设置页原本用绝对坐标 `Location=(16,70)` 摆放，被 `Dock=Top` 的 PageHeader(64px) 遮挡。改用 `TableLayoutPanel` 自适应布局（左列：设备控制/网络设置/系统维护；右列：运行统计/轮询设置）。另：硬件监控页补 MCU 型号(32.1.1)+系统时钟(32.1.2)两张卡（之前只有13路传感器）。
- **嵌入式端 MIB 扩展**（app/snmp/mib.c）：
  - `32.2.1/2/3` (netIp/netMask/netGw) 改为可读写，新增 `snet_ip/snet_mask/snet_gw`（写回 g_netcfg + netcfg_save，与 web /api/network 一致；重启生效）。
  - 新增 `32.4.3 ctrlReset`：Get 返回 0，Set=1 时 netcfg_save 后 NVIC_SystemReset() 软复位（与 web api_reset 一致）。
  - mib.c 加 `#include "stm32f4xx_hal.h"` 取 NVIC_SystemReset。
  - SnmpEngine.cs 加 Oids.System.Clock、Oids.Control.Reset、SetString()、ResetDevice()。
- **真机联调受阻（重要）**：烧录后发现设备网络 100% 丢包（ping 不通、SNMP 无响应）。为排除本次改动，对照实验把 MIB 恢复成"会话前基线"（text 完全一致 284488B）重烧，仍 100% 丢包 → **确认网络不通与本次 SNMP 代码改动无关**，是设备/链路层既有问题（PHY/线缆/供电）。最终功能固件（text 285000B，含可写网络+复位）已烧录，但端到端 Set 网络/复位/硬件页刷新未能在真机验证（设备网络当前不可达）。
- **逻辑验证**：headless 回环测试 SetString 构造的 SetRequest 报文（OID=32.2.1, value=192.168.10.200）可正确解析；Reset/Clock OID 字符串正确 → 7/7 PASS。Get/Walk/SetInt/LED/BEEP 链路此前设备可达时已 10/10 PASS。
- 待设备网络恢复（检查以太网线缆/PHY/供电）后，可重新跑 snmp_verify + snmp_desktop 做完整端到端验收。

## 第三十九波：硬件修复后真机端到端验收（全 PASS）
- **设备网络恢复**（用户确认硬件已修复）：ping 192.168.10.99 4/4 收到 0% 丢包。
- **根因修复（之前 readback IP 失败）**：原 `gnet_ip/mask/gw` Get 读 `g_sta.ip`（hwinfo_init 启动时的一次性快照），而 SNMP Set 写的是 `g_netcfg.ip`，两者不同步 → readback 永远是旧值（Mask/GW 测试值恰好等于当前实际值造成假阳性 PASS）。
  - **修复**：`gnet_ip/mask/gw` 改为直接 `sscanf(g_netcfg.ip/mask/gw,...)` 实时读取，不再经 hwinfo 静态快照。
  - 同步：verify 脚本 Test9 复位判据改为"Set 请求已发出即 PASS + 复位后 uptime 归零确认"（设备 NVIC_SystemReset 在发响应前发生，不一定收到 SetResponse）。
- **验收结果**：
  - 固件重编零警告零错误（FLASH 285000B / 27.23%，RAM 85984B / 43.73%），openocd 烧录 Verified OK。
  - `tools/snmp_verify` 端到端 **31/31 PASS**：Get 系统/网络、Walk 27 节点、Set LED/BEEP 读写、Set 网络 IP/Mask/GW 后 readback 实时生效（修复后真 PASS，非假阳性）、错误 community 丢弃、Set 复位→设备重启→SNMP 恢复+ uptime 归零。
  - `tools/snmp_desktop_headless`（新增，复用真实 SnmpEngine）**15/15 PASS**：四页（系统概念/硬件监控/网络状态/参数设置）数据通路全通，13 路传感器真实读数（lux=4 ps=23 ir=8 ax=4 ay=0 az=100 gx=-6 gy=30 gz=-195 valid=1），Set LED/BEEP 读写，Walk 27 节点。
- **结论**：SNMP 功能（含可写网络参数 + 系统复位）在真机端到端全部验证通过，硬件链路已确认正常。

## 第四十波：snmp_desktop UI 重构（传感器独立页 + 参数设置页硬件信息区）
- **用户需求**：① 参数设置页要显示硬件信息（MCU/时钟/传感器）；② 传感器信息单独成页；③ 右页内容向下移动全部显示（之前被裁切）。
- **改动**（tools/snmp_desktop/MainForm.cs）：
  - 左侧菜单 4 项→5 项：系统概念/硬件监控/**传感器监控**/网络状态/参数设置（index 映射同步更新 0..4）。
  - 新增 `pnlSensors` 独立页 `BuildSensorsPage()`：整页 FlowLayoutPanel 网格（13 路传感器卡，220×92，AutoScroll 可滚动），与硬件监控页解耦。
  - 硬件监控页简化为硬件概要：MCU 型号(32.1.1) + 系统时钟(32.1.2) + 运行任务数(32.1.3) 三张卡（去掉原底部传感器网格）。
  - 参数设置页重构：外层 `Panel(Dock=Fill, AutoScroll=true)` 包裹 TLP（Top=64 起），**内容超长出现垂直滚动条，保证全部显示**（修复之前 LED/BEEP 被裁切 + 网络设置/系统维护看不到的问题）。
    - 右列新增"硬件信息"卡：MCU 型号 + 系统时钟 + 13 路传感器实时值列表（每行 label+value，13 行）。
    - 右列顺序：硬件信息卡(360×440) → 运行统计卡 → 轮询设置卡；左列：设备控制 → 网络设置 → 系统维护。
  - 新增字段：`valSetMcu/valSetClock`、`_setSensorVals[13]`、`_hwTasks`；`RefreshSensors()` 单独刷新传感器页；`RefreshSettings()` 同步刷新硬件信息区；`SwitchPage` 加 `pnlSensors.Visible`。
- **验证**：`dotnet build -c Release` 0 警告 0 错误；`tools/snmp_desktop_headless` 扩展验证 **24/24 PASS**（五页数据通路：系统/硬件/传感器/网络/参数设置含硬件信息区 + LED/BEEP Set 读写 + Walk 27 节点）。传感器真实读数（lux=0 ps=15 ir=8 ax=3 ay=0 az=99 gx=-18 gy=30 gz=-195 valid=1）。

## 第四十一波：snmp_desktop 全量布局重写 + 视觉测试工具（裁切彻底修复）
- **用户反馈**：传感器页用 FLP 单列排版（13 张卡全宽堆叠），参数设置页 LED/BEEP 仍被裁掉（用户标注另外两个页面也裁切）。要求：① 传感器用独立菜单页 + 多列网格；② 设备信息单独成页；③ 实现后**直接打开测试**，确保每个页面都显示正常。
- **重构 MainForm.cs（绝对定位彻底替代 Dock/TLP/FLP/SplitContainer）**：
  - 左菜单 6 项（系统概念/设备信息/硬件监控/传感器监控/网络状态/参数设置），自绘 Panel+Label（避免 ListBox OwnerDrawFixed 不出滚动条），`AutoScroll=true` 防菜单被裁。
  - 右侧 `pnlContent` 绝对定位 (200,92)，每页 = `PageHeader(Dock=Top, H=64)` + `MakeScroll()`（绝对 Location=(0,64), Size=(pnlContent.W, pnlContent.H-64), AutoScroll=true），所有内容纵向滚。
  - `Resize` 事件回调 `SyncScrollSizes()` 同步滚动面板尺寸。
  - `BuildDevicePage`：12 张全宽卡（MCU 型号/UID/HSE/PLL M/N/P/Q/SYSCLK/HCLK/PCLK1/PCLK2/Flash/SRAM），RefreshDevice 读 System.Descr + System.Clock。
  - `BuildSensorsPage`：13 张卡按 `ClientSize.Width-200-2` 自适应 3 列网格（780px 宽/列~245px），`cardH=80, gap=12, pad=16`，`cols = Math.Max(1, (usableW - pad*2 + gap) / (220 + gap))`，上限 4。
  - `BuildSettingsPage`：6 张卡（设备控制 LED/BEEP ToggleSwitch、网络设置 IP/Mask/GW+应用按钮、系统维护 复位按钮、硬件信息 MCU/Clock+13 传感器、运行统计 Req/Err/LastUpd、轮询设置 NumericUpDown），均用绝对 `MakeCard(y)` 累加。
- **新工具 snmp_desktop_visualtest（视觉测试 + 自动截图）**：
  - `tools/snmp_desktop_visualtest/VisualTest.csproj`：net9.0-windows, 引用 MainForm.cs/ToggleSwitch.cs/SnmpEngine.cs/SnmpCore.cs（不需 build SnmpDesktop.exe）。
  - `Program.cs`：实例化 `MainForm(ClientSize=980×640)` → 反射填 `txtHost=192.168.10.99, txtPort=161` → `ClickMenu(reflect SelectMenu(idx))` 切 6 页 → **P/Invoke `PrintWindow(hwnd, hdc, PW_RENDERFULLCONTENT=2)`** 截 PNG（对抗锯齿/OwnerDraw 完整支持，远优于 DrawToBitmap 失真）。`DumpSensors(form)` 反射 dump 卡片 (X,Y) 网格。
  - 编译期一度 CS0246 缺 `using System.Collections.Generic;`（List<string>），补全后 0 警告 0 错误。
- **验收**：visualtest 跑通后 `cardCount=13 positions=(16,16)(268,16)(520,16)(16,108)...` 即 3 列×5 行（含最后 1 张单卡），与预期一致。
- **新工具 verify_snmp_desktop_visual.py**：
  - 调用 visualtest.exe → 逐 PNG 用 Pillow 灰度图统计亮像素 (>200) 占比 >35% 才视为「过亮/可能白边」（深色主题下正常仅 5-8%）。
  - 解决 C# 控制台 cp936 GBK 输出：`subprocess.run(text=False)` + `.decode("gbk", errors="replace")`。
  - **6/6 PASS**：系统概念 5.1% / 设备信息 7.8% / 硬件监控 5.2% / 传感器监控 5.2% / 网络状态 5.4% / 参数设置 6.5%。
- **发布**：`dotnet publish -c Release -r win-x64 --self-contained false` → `tools/snmp_desktop/publish/snmp_desktop.exe` 197KB 单文件。
- **结论**：6 个页面均完整渲染，13 路传感器在 980×640 下自动 3 列网格，scroll/clip 行为符合预期，硬件信息单独成页，LED/BEEP/网络/复位/13 传感器全部可见可交互。

## 第四十二波：SNMP 硬件数据运行后变全 0 的根因与修复
- **现象**：snmp 运行一段时间后硬件数据（13 路传感器）全为 0 且不再更新，网页 / shell / snmp 三处都变 0。
- **根因（双层面）**：
  1. **运行时 I2C 总线锁死无恢复**：hwinfo_task 每 200ms 经 web_i2c_lock 读 AP3216C/MPU9250（HAL_I2C_Mem_Read 仅 10ms 超时）。一旦从设备把 SDA 拉低锁死（噪声/复位中途打断事务），HAL 读永久超时返回 -1，collector 持续发布 0 + sensor_valid=0，且**无看门狗、无总线恢复** → 数据永久冻结为 0。
  2. **启动期 I2C 卡死（本次闪烧后暴露）**：烧录后用 openocd `reset halt;program;reset` 打断在飞 I2C 事务，留下 SDA 锁死。下次启动 `main()` 在调度器启动前调 `BSP_ETH_PHY_Reset()→BSP_PCF8574_Write→HAL_I2C_Master_Transmit`，因 BUSY/SDA 低卡在 `I2C_WaitOnFlagUntilTimeout`；而 HAL tick（TIM7）在调度器启动前不推进，`HAL_GetTick()-Tickstart>Timeout` 永不成立 → **无限死等**，整板起不来（ping 100% 丢包）。GDB 栈：`main:82 BSP_ETH_PHY_Reset → BSP_PCF8574_Write → HAL_I2C_Master_Transmit → I2C_WaitOnFlagUntilTimeout`。
- **修复**：
  - `bsp/bsp_i2c.c` 新增 `BSP_I2C_Recover()`：检测 SDA 低 / SR2.BUSY / SR1 错误标志 → 把 SCL 配成 GPIO 推挽翻转 ≥9 次释放被卡从设备（读 SDA 提前退出）→ 发 STOP → `HAL_I2C_DeInit+Init` 清锁存错误 → 还原 AF_OD 引脚 → 复测 BUSY/SDA。纯 `__NOP()` 延时，**调度器启动前也可安全调用**。
  - `app/main.c`：`BSP_I2C_Init()` 后、`BSP_ETH_PHY_Reset()` 前插入 `BSP_I2C_Recover()`，启动即清锁死总线（实测：此前卡死起不来的板子加此调用后正常启动）。
  - `app/hwinfo.c`：hwinfo_task 读失败（ok1/ok2!=0）即调 `BSP_I2C_Recover()` 重试一次，仅恢复后仍失败才发布 0 + valid=0；新增 `i2c_recover` 计数（g_dyn 累加并跨周期保留）。任务栈 256→384（容纳 DeInit/Init 栈深）。
  - `app/snmp/mib.c` 新增 `32.5.4 statI2cRecover`（Counter32，读 g_dyn.i2c_recover）；`app/shell.c` `cmd_dev` 打印 `i2c_recover`。
  - `tools/snmp_desktop/SnmpEngine.cs` 补 `Oids.Stats.Recover`；`tools/snmp_desktop_headless/Program.cs` 扩展：读 32.5.4 + 10 次稳定性轮询（sensor_valid 须恒为 1）。
- **验证**：
  - Debug + Release 双构 **0 警告 0 错误**（FLASH 286292/310808B，RAM 85992B/43.74%）。
  - openocd 烧录 Release **Verified OK**；设备从卡死（ping 100% 丢）恢复在线（0% 丢）。
  - `snmp_desktop_headless` 扩展 **26/26 PASS**；稳定性轮询 sensor_valid=1、lux 随环境波动（1→3），无冻结。
  - `curl /api/hardware` 实时非空：`ap3216c{lux,ps,ir,valid:1}`、`mpu9250{...}`；30s 浸泡 valid 恒为 1。
  - `snmp_desktop_visualtest`：13 路卡 3 列网格正常渲染，数值为实时读数。
- **结论**：根因是 I2C 总线锁死后无恢复（运行时 + 启动期两处）。新增 `BSP_I2C_Recover()` 并在采集任务与启动路径各接一处，彻底消除"数据变 0 不再更新"；新增 32.5.4 恢复计数便于现场观测。
