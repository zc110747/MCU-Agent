# 2026-08-31

## 201.esp32s3_rtos — 补齐驱动模块并编译通过

- 项目为 ESP32-S3 N16R8 FreeRTOS 多任务监控器（Arduino + ESP32 Core 内置 FreeRTOS）。
- 问题：`.ino`/各 task 引用 `drivers/led.*`、`drivers/button.*`，但工程缺 `drivers/` 目录（git 历史从未纳入），编译必失败。
- 按用户约定**改用 `bsp/` 模块**（规避与 STM32 `Drivers/` 命中 `.gitignore` 的 `**/Drivers` 规则）。已 `git check-ignore` 确认 `bsp/led.cpp` 不被忽略。
- 新建 `bsp/led.{h,cpp}`（led_init/led_set/led_toggle，依据 config.h 的 LED_PIN/LED_ACTIVE_HIGH）、`bsp/button.{h,cpp}`（button_init/button_poll_pressed，3 采样去抖 + 边沿检测，每次物理按下仅报一次）。
- 替换所有 `drivers/` include 为 `bsp/`：`.ino`、`app/app.cpp`、`tasks/task_{led,button,uart}.cpp`；同步 README 结构图与分层说明。
- 编译环境：arduino-cli `D:\data\agent-tools\arduino-cli_1.5.2-rc.1_Windows_64bit\arduino-cli.exe`，core `esp32:esp32@3.3.11`（data 目录 `D:\software\arduino-cli\data`）。
- 命令：`arduino-cli compile -j 8 -b "esp32:esp32:esp32s3:PSRAM=opi,FlashSize=16M,PartitionScheme=default,UploadSpeed=921600" --build-path ".build" "."`
- **[BUILD] PASS**：0 error / 0 warning；程序 314768 B (24%)，动态内存 22880 B (6%)。产物 `.build/*.bin`/`.elf` 生成正常。
- [FLASH]/[RUN] 未烧录（无硬件连接），需用户接 CH343 + ESP32-S3 N16R8 后 `build.bat flash` 验证。
