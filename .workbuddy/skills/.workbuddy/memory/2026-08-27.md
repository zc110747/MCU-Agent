# 2026-08-27 工作日志

## 更新 skill：stm32-project-scaffold（CMakeLists.txt 约束）
- 依据 `010.stm32h743_boot/CMakeLists.txt` 的修改，给 `stm32-project-scaffold` 新增 CMakeLists.txt 约束：
  - **2.1 CMSIS Device 启动文件必须显式列出，禁止 `GLOB_RECURSE`**：Device 树含 ~60 个 startup.s（arm/gcc/iar×各 H7 型号）+ 多 system_*.c，递归 glob 会重复定义 `Reset_Handler`/`SystemInit` 并触发 iar/arm 语法 .s 被 GCC 汇编报错。改为只列 `startup_stm32h743xx.s`(gcc) + `system_stm32h7xx.c`。
  - **2.2 HAL 源文件收集用 Cube 标准子目录名** `Drivers/STM32H7xx_HAL_Driver/`（F4 对应 STM32F4xx_HAL_Driver/），并 `list(FILTER ... EXCLUDE REGEX "[Tt]emplate")` 排除 CubeMX 模板。
  - 目录分层图 `Drivers/` 行补充 Cube 标准 HAL 子目录名；frontmatter 触发词增加 CMSIS/GLOB_RECURSE/HAL 命名等。
- 同步更新用户级全局副本 `C:\Users\zc110\.workbuddy\skills\stm32-project-scaffold\SKILL.md`（diff IDENTICAL）。
