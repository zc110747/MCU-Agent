# 2026-08-29

## 任务：梳理全部任务 README/memory 并据此更新 skills（项目级 + 全局同步）

### 梳理范围
- 项目级 README：001–011、101、102、201、202（共 14 个任务，README 合计 ~260KB；另含大量 third_party 噪声 README 已排除）。
- 各任务 `.workbuddy/memory/`：002/003/005/007/008/010/101/102/202 有 `MEMORY.md`（蒸馏长期记忆）；root `.workbuddy/memory/` 有 2026-08-23/26/27/28 工作日志；skills 内存日志 2026-08-27。
- 能力表：`STM32_skills_能力表.md`（全局索引）。

### 提炼并落地的可复用约束（按 skill）
1. **stm32-project-scaffold**（+3112B）
   - 新增 **第十四节 sys_startup 本地设备层约定**（替代 `Drivers/CMSIS/Device`，已迁移 002/003/004/005/006/007/008/010，验收红线 `Drivers/CMSIS/Device` 出现次数==0）。
   - 第十节补 **致命坑 4**：`cd /d "%~dp0"` 尾随反斜杠 + `for` 块 `2>&1` → `此时不应有 .`；并明确 `.bat 纯英文`。
2. **stm32-peripheral-drivers**（+2128B）
   - §3.1 H7 DCMI 基地址 `0x48020000`(AHB2)、极性 RISING/LOW/LOW、彩条模式、`drv_dcmi.c` 多缓冲防撕裂。
   - §5.6 SDIO 4 字节对齐 + exFAT 字库挂载。
   - §8.4 F429 LCD 8080（NT35510 扫描方向交换→GRAM 480×800、LVGL 画布=480×800、MIPI-DCS 地址窗口时序）。
   - §8.5 GT911 触摸（软件位绑定 I2C、184B 配置块禁给 GT911、PH7 中断风暴、EXTI_SWIER 注入验证）。
3. **stm32-ai-dev-environment**（+~700B）：双 openocd 实例端口冲突（tasklist/netstat 查 PID）、PowerShell stderr→红色误报。
4. **stm32-verification-acceptance**（+~1100B）：openocd 烧录必须用 `.elf`（.bin 报 no flash bank found）、`mdw` 4 字节对齐直读、「没报错≠有数据」正向验证（nm+mdw）、LIBUSB_ERROR_ACCESS / COM code-31 排查。
5. **zephyr-stm32-porting**（+~1000B）：CMakeLists `find_package(Zephyr)` 前工具链自动探测（ZEPHYR_TOOLCHAIN_VARIANT/gnuarmemb）、`west build -b` 全新构建、build_oneclick if-exist-build、PowerShell west stderr 误报。
- **stm32-vibe-coding-workflow**：本次无需改动（已覆盖完整方法论）。
- 同步更新 `STM32_skills_能力表.md` 对应 5 行「一句话能力 / 何时用」。

### 同步结果
- 5 个 skill 均 `cp` 到 `~/.workbuddy/skills/`（`C:\Users\zc110\.workbuddy\skills\`）并 `diff` 校验 **IDENTICAL**。
- 项目级与全局两份副本一致。
