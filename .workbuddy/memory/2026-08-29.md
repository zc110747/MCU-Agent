# 2026-08-29 工作日志

## 全仓 README + memory 梳理 + skills 更新 + 项目级/全局同步

### 一、梳理（README + memory 审计）
- 通读全部 14 个任务根 README（001–011 / 101–102 / 201–202）与 9 个项目 curated MEMORY.md。
- 发现根 README 过时：缺 011(emWin OLED)、102(F429 USB Host+exFAT+LVGL+GT911)、202(ESP32-S3 USB RNDIS)；201 被误标进 STM32F429 区块（实为 ESP32-S3）；存在重复的 "STM32F429IGT6项目" 标题。
- 确认 6 个 STM32 复用型 skill 中 5 份工作区/全局副本已一致，仅 `stm32-vibe-coding-workflow` 有 1 行差异（全局副本残留硬路径/项目计数）；另有 2 个全局专属互补 skill（`stm32-logging-print-log`、`stm32-swd-forensics`）。

### 二、更新 skills（沉淀新项目经验）
- `stm32-ai-dev-environment`：新增「六、预编译厂商库与工具链版本锁定（binutils 2.44 坑）」——STemWin 预编译 .a 在 ld 2.44 下链接中止，锁定 binutils<2.44（13.3/14.2 rel1）。
- `stm32-peripheral-drivers`：① §8.5.1 新增 GT911 中断风暴三层防护（源头/隔离/容错，来自 102）；② §十 新增 emWin GUI 栈要点（CRC 时钟、ARGB 色序、.a 链接、同 003 字库）；③ §十一 新增 USB Host(TinyUSB)+真正 exFAT（OS 启动后 init、SDRAM 先于 RTOS 对象、FatFs 并发锁）。frontmatter 增触发词。
- `stm32-verification-acceptance`：§4.3 新增 PRINT_LOG 全局日志开关 + SWD 验证开关行为（102 的 verify_log_switch.py 6/6）。frontmatter 增触发词。
- `stm32-vibe-coding-workflow`：§八 新增跨平台延伸（ESP32-S3，arduino-cli/ESP-IDF，同套验收方法论）。frontmatter 增触发词。
- `STM32_skills_能力表.md`：表头补充 2 个全局互补 skill；4 行能力描述补新主题；清理清单项目计数更新为 14 个任务。
- 同步：工作区 6 份 SKILL.md `cp` 覆盖全局副本，全 SYNC（顺带修正全局 vibe 残留硬路径）。

### 三、修正根 README
- 项目清单补 011/102/202；201 移入 esp32s3 区块（芯片误标修正）；删重复 F429 标题；新增 011/102/201/202 详情块（README 链接 + AI开发/个人参与 + 工作量）。

### 四、同步 memory
- 项目级：新建今日日志 `2026-08-29.md`。
- 全局：新建 `~/.workbuddy/MEMORY.md`（跨项目偏好：工程分层铁律、零警告、端到端验收、相对路径、sys_startup、批量 .bat 约定、UI 深色风格、AI Agent 工作流）。
