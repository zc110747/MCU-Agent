# 2026-08-23 工作日志

## 整理 md 文档 → 生成嵌入式开发 skills

扫描工作区自有 md（排除 third_party/lib/node_modules/.git），提炼出一套 STM32 嵌入式
AI 开发 skills，写入 `~/.workbuddy/skills/`（用户级，跨项目复用）：

1. **stm32-vibe-coding-workflow** — AI Agent 嵌入式总方法论（环境/提示词模板/分阶段验收/人工干预）
2. **stm32-project-scaffold** — 工程骨架（app/bsp/Drivers/third_party + CMake+Ninja+OpenOCD）
3. **stm32-ai-dev-environment** — 工具链环境验证与 Windows/GitBash 坑（west 调用、GBK、swd）
4. **stm32-peripheral-drivers** — H7/F4 引脚速查 + OV5640/ST7789/SD-GBK字库/QSPI/USB-VDD33USB/I2C恢复
5. **stm32-verification-acceptance** — 端到端验收（双构零警告/烧录/串口网络/verify 脚本 pass-fail）
6. **zephyr-stm32-porting** — Zephyr3.7+LVGL8.4 移植（overlay/时钟树/DISPON patch/GBK双段/shell）

源文档：根 README.md、document/{support,stm32h7_hw,stm32f4_hw,prompter}.md、各项目
README.md/prompter.md、008 VERIFICATION.md。已有 stm32-bare-metal-bringup 保留不重复。
每个 skill 含 YAML frontmatter（name/description/agent_created）+ 结构化中文正文 + 触发词。

## 备份 skills 到 workspace
将本次生成的 6 套 skill 备份到 `E:/cnb/git/Mcu_Project_Design_By_Agent/.workbuddy/skills/`
（项目级，与用户级 `~/.workbuddy/skills/` 互为镜像）。frontmatter 完整、结构一致。
注：已有的 `stm32-bare-metal-bringup`（用户级）本次未纳入备份，仅备份本次新生成的 6 套。
