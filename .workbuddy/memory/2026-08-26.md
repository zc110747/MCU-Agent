# 2026-08-26 工作日志

## STM32 skills 去硬路径清洗 + 全局能力表
- 清洗 6 个 STM32 skill，去除本机硬路径/项目专属内容：COM19/COM3/COM4 → "端口号依本机分配（如 COMx）"；`D:/Software/openocd/...` → 占位符 `<openocd 安装目录>/...`；`D:/user_project/coding_git/.../7.stm32h7_iap` 外部参考绝对路径删除（仅保留样本名）；`support_tools/env_support_for_stm32h743.zip` 等专属包名 → 通用"打包 Drivers/third_party 随工程分发"；`9/11 个项目（001–010+101）` → "多个 STM32 项目"；悬空引用 `stm32-bare-metal-bringup`（不存在的 skill）→ 替换为实际存在的 6 个 skill 交叉引用。
- 以项目级（`.workbuddy/skills`）为权威源（比用户级副本更新更完整，已移除悬空引用并扩充 Bootloader/gdb 验证经验），清洗后双写同步到用户级（`~/.workbuddy/skills`），两份现已 `diff` 完全一致。
- 保留全部流程与经验参考：H7/F4 引脚速查表、UNIGBK 双段 GBK 字库结构、Bootloader Flash 引擎坑（DTCM 不可执行/双 Bank/跳转序列）、gdb 函数级验证与挂死定位、双固件镜像验收等。
- 新增全局能力表 `STM32_skills_能力表.md`（用户级 + 项目级各一份），汇总 6 个 skill 的能力/触发场景/配套关系，并附硬路径清理审计清单，作为对外分享的选型入口。
