# 2026-08-27

## 统一 12 个 STM32 工程的 .vscode 构建/仿真配置

对 `001~010`、`101`、`102` 共 12 个工程做了 `.vscode` 构建/仿真内容统一：

- **tasks.json** 统一为仅 `configure / build / clean / flash` 四个任务，全部使用裸程序名（`cmake` / `openocd` / `arm-none-eabi-gdb`，不加路径、不带 `.exe`）。
  - 构建机制按工程现状保留：预设工程(`001/004/005/006/007`)走 `cmake --preset debug`；普通 CMake 工程(`002/003/008/010/101/102`)走 `cmake -S . -B build -G Ninja -DCMAKE_BUILD_TYPE=Debug`；`009`(Zephyr)走 `python -m west build`。
- **launch.json** 改为引用工程根级 `openocd.cfg` + 根级 `*.svd`，`openocdPath`/`gdbPath` 用裸名；`007` 额外保留“经自研探针调试 F429”的 `openocd_cmsisdap.cfg` 配置。
- **settings.json** 统一为裸路径（`cortex-debug.openocdPath=openocd`、`gdbPath=arm-none-eabi-gdb`、cmake generator=Ninja）；`102` 的 `c_cpp_properties.json` 里 `compilerPath` 的 `${env:...}` 已改为 `arm-none-eabi-gcc`。
- **svd/cfg 归位**：各工程散落的 `*.svd`、`*.cfg` 全部移到工程根目录（与 `.vscode` 同级），删除已清空的 `openocd/` 子目录；`004/005/006` 缺 `openocd.cfg` 已补建统一 H743 版。
- 全 12 工程 `tasks/launch/settings` JSON 结构校验通过；`openocd.cfg` 解析校验通过（仅缺探针的 adapter 报错，需真机）。
- 全量 `configure+build` 编译验证已在后台运行（task 96ppYC，日志 `build_all.log`），待完成回报 per-project pass/fail 与警告数。

注：用户称“放在项目中prompter同级”按“工程根目录”理解（消除 openocd/ 子目录）。

## 将统一规则整理成规范文档 + 沉淀进 WorkBuddy 技能

- 工程目录新增 `vscode_unify_convention.md`（仓库内人工/AI 查阅的 .vscode 统一规范，含三条铁律 + 模板 + 验收方式）。
- 扩展 WorkBuddy 技能 `stm32-project-scaffold`：纠正旧版「`openocd/` 子目录 + `<openocd 安装目录>` 绝对路径」写法，新增第九节「多工程 .vscode 批量统一规范」（裸工具名 / 仅 4 任务 / svd·cfg 工程根 / 排除 .gitignore 检索 / 构建机制矩阵 / 验证脚本思路）。
- 同步两份技能副本（用户级 `C:/Users/lx176/.workbuddy/skills/` 与工程级 `D:/user_project/.../.workbuddy/skills/`），并同步更新两份 `STM32_skills_能力表.md` 的 `stm32-project-scaffold` 行。

## 为每个项目生成一键编译脚本 build_oneclick.bat

在 14 个工程根目录各生成 `build_oneclick.bat`（含 `201/202` ESP32；脚本名 `build_oneclick.bat` 以避开 201 已有的 `build.bat`）：

- **全英文输出**；所有出口（成功/失败/abort）均 `goto :END` → `pause` → `exit /b %ERR%`。
- **ST 工程**（`001~010`、`101`、`102`，`009` Zephyr 除外）步骤 1 检查工具 `cmake`/`ninja`/`openocd`/`arm-none-eabi-gcc`（缺失则提示参考 `document/support.md` 安装），步骤 2 检查根目录 `Drivers` 与 `third_party` 是否存在，缺失则提示从 `..\support_tools\env_support_for_stm32h743.zip`（F429 用 `env_support_for_stm32f429.zip`）解压/拷贝；步骤 3 `configure→clean→build`。
  - 预设工程(`001/004/005/006/007`)：`cmake --preset debug` + `cmake --build build/debug --target clean` + `cmake --build build/debug`。
  - 普通 CMake(`002/003/008/010/101/102`)：`cmake -S . -B build -G Ninja -DCMAKE_BUILD_TYPE=Debug` + `cmake --build build --target clean` + `cmake --build build`。
- **009 Zephyr**：检查 `cmake/ninja/openocd/arm-none-eabi-gcc/python`；步骤 `west build -t clean`(warn) → `python -m west build -b nucleo_h743zi/stm32h743xx -d build -s .`（west 将 configure 集成进 build，无本地 Drivers/third_party 检查）。
- **201 ESP32(Arduino)**：检查 `arduino-cli` → `arduino-cli compile -j 8 -b <FQBN esp32s3 N16R8> --build-path .build .`。
- **202 ESP32(ESP-IDF)**：检查 `idf.py` → `idf.py set-target esp32s3`(warn) → `idf.py build`。
- 14 个脚本全部通过结构校验（英文/工具检查/ST 目录检查/support_tools 引用/构建命令/pause 出口）；`001` 的 bat 内封装命令经直接运行验证：`configure/clean/build` 均 exit=0，产出 ELF。沙箱禁止从 Bash/PowerShell 启动 `cmd.exe`，故未双击执行 .bat，但脚本仅包裹上述已验证命令。

## 修复 build_oneclick.bat 的 `:!MISSING!` 字面量 bug

双击运行报错并出现 `:!MISSING!`：根因是工具检测分支用 `!MISSING!` 延迟展开打印缺失工具名，一旦该分支触发且延迟展开未生效即原样打印。

- 重写全部 14 个 `build_oneclick.bat`：移除 `setlocal enabledelayedexpansion` 与任何 `!VAR!`，改为 `for %%T` 循环逐个 `where %%T`，缺失时直接 `echo [ERROR] Required tool not found: %%T`，用 `set "TOOLMISS=0/1"` 标记、顶层 `%TOOLMISS%` 判定；结尾 `if %ERR%==0 (...) else (...)`。
- 修复 Python 生成陷阱：原本 `"for %%T in (%s)" % tools` 中 Python `%` 格式化把 `%%` 吞成单 `%`，导致 `for %T` 语法错误；改为字符串拼接 `"for %%T in (" + " ".join(tools) + ") do ("`（已用 `od -c` 确认第 19 行为双 `%%`）。
- 14/14 结构校验通过（无 `!MISSING!`/`!ERR!`、for 循环 `%%T` 正确、括号平衡、英文、ST 含 Drivers/third_party 检查、出口 pause/exit 完整）。
- 技能 `stm32-project-scaffold` 新增第十节「一键编译 bat 规范（修正版）」，记录正确模板 + 两个坑（勿用 `!MISSING!`、Python 生成时 `%%`→`%`），用户级与工程级两份副本同步。

## 在 cmd / PowerShell 实测 build_oneclick.bat 并修复 `& ( )` 解析 bug

用户要求：在 cmd 和 powershell 上都测试，先验证 `001` 通过后再改其它。实测环境与结论：

- **沙箱限制**：Bash/PowerShell 工具均禁止启动 `cmd.exe`（"Invoking cmd.exe ... bypasses command validation"，即便关沙箱也拦）。唯一可运行 `.bat` 的方式是 PowerShell 工具原生 `& ".\xxx.bat"`（bat 本就由 cmd.exe 解释执行，故等效 cmd 上下文）。直接 `cmd.exe /c` 始终被策略拦截。
- **真实 bug（双击报错根因）**：实测 `001` 的 bat（上一版仅修了 `!MISSING!`）仍报 `此时不应有 into。`——来自目录检查 `echo` 行里**行首的 `(`**：`echo (or copy ..\support_tools\...\Drivers) into this folder` 被 cmd 当成命令组，遇 `)` 提前结束，余下 `into this folder` 成非法命令。后续又发现 `009` 的 `echo [STEP] Configure & Build ...` 里 **`&`** 被当命令分隔符，报 `'Build' 不是内部或外部命令`。
- **修复**：在生成器对所有 `echo`/`REM` 行做清洗——`&`→`and`、`( ) | < >`→删除（仅清洗这两类行，`if/for/cmake` 命令行的括号保留）。反斜杠 `\`（路径）在 cmd echo 中是字面量不受影响。
- **验证结果（全部在 cmd 解释器下）**：
  - `001`（STM32 CMake 预设）：clean build，日志无 `此时不应有`、含 `[DONE] Build succeeded.`、ELF 重建、exit 0。✅
  - `009`（Zephyr/west）：完整 `west build` 成功，`[DONE] Build succeeded.`，无 `当时不应有`/无 `'Build' 不是内部`。✅
  - `201`（ESP32-Arduino）：`arduino-cli` 存在→下载索引并尝试编译→仅因本机未装 `esp32:esp32` 平台失败（环境，非脚本），脚本正确捕获打印 `[ERROR] Build arduino-cli compile failed.`，无解析错误。✅
  - `202`（ESP32-IDF）：`idf.py` 缺失→工具检查正确打印 `[ERROR] Required tool not found: idf.py` 并 pause。✅
- 14 个 `build_oneclick.bat` 已全部用同一模板重生成；Python 确定性扫描确认所有 `echo/REM` 行不含 `& ( ) | < >`（仅有 `\` 路径，无害）。
- 测试产物（`ps_test.log`/`cmd_test.log`/`probe.log`、临时 `.fix_bat2.py`）已清理。
- 技能 `stm32-project-scaffold` 第十节补充**致命坑 3**：`echo`/`REM` 行里的 `& ( ) | < >` 在 cmd 下全部危险，必须清洗（含 Python `sanitize()` 示例）。用户级与工程级两份副本同步。
- 注：本机 `west`/`arduino-cli` 已安装；`idf.py` 未安装；`009` 完整 Zephyr 构建可跑通。

## 工作空间级一键编译所有项目脚本 build_all.bat

在仓库根目录（所有工程的上一层）新增 `build_all.bat`，一键顺序编译全部 14 个工程：

- 遍历固定列表（`001~010`/`101`/`102`/`201`/`202`）逐个 `call "<proj>\build_oneclick.bat" < nul`：`< nul` 把子脚本末尾的 `pause` 喂 EOF 使其立即返回（不再阻塞），父脚本据此统一控制暂停时机。
- **出错才暂停**：任一工程 `exit /b` 非 0 → 打印 `[ERROR]`，`pause` 等待用户回车后继续后续工程；成功则直接继续（不打断）。缺 `build_oneclick.bat` 的工程标记为 `[SKIP]`。
- 末尾输出汇总 `Passed / Failed / Skipped` 计数，并 `pause` 等待回车退出（避免双击后窗口直接关闭丢失结果）。
- 控制流用 `:build` 子例程（避开 `for` 内 `goto` 会中断循环的 cmd 老坑）；计数器用 `enabledelayedexpansion` + `!VAR!`。
- 验证：① 用 3 个 dummy 子脚本（成功/失败/缺失）做控制流测试，确认 `< nul` 跳过子 pause、errorlevel 正确回传、失败暂停后继续、汇总计数正确；② 对真实 `001` 工程 `call ... < nul` 实测：`cmake` configure→clean→build 全过，产出 `h743_tinyusb_cdc.elf`(FLASH 65668B/2MB=3.13%)，`ERRLEV=0`，证明 `< nul` 不干扰 cmake/ninja 且退出码正确回传。
- 临时测试夹具（`D:/tmp_bat_test`）已清理。

## 编写 001 / 003 工程 README + 统合进 skills

按用户要求，为两个工程补全根目录 `README.md`（此前为空），并把「编译脚本体系 + README 文档规范」沉淀进 `stm32-project-scaffold` 技能（用户级与工程级两份副本同步）。

- **001 README**（`001.stm32h743_tinyusb_cdc_msc/README.md`）：基于 `prompter.md` 与 `CMakeLists.txt` 实际内容，写清 CDC+MSC 复合设备、硬件（H743/HSE25M/OTG_FS(PA11/12)或 HS(PB14/15)/SD/FatFs/SWD）、工程结构、开发流程（先 CDC 再 MSC）、构建（build_oneclick.bat / 仓库根 build_all.bat / `cmake --preset debug`→`build/debug`，实测 FLASH 3.13%）、调试烧录（launch.json 裸名 + 根 openocd.cfg + STM32H743.svd、命令行 program、gdb 脚本）、验收与 PC 端 `test_*.py` 自测、常见问题表。
- **003 README**（`003.stm32h743_lvgl_oled/README.md`）：LVGL 中文 OLED（ST7789 240×240 SPI6、SD 卡 `1:/SYSTEM/FONT/` GBK 字库、调试串口 PA9/PA10 115200、HSE25M），结构（Core/Bsp/Application/Drivers/third_party(lvgl+FatFs)/根级 STM32H743ZITX_FLASH.ld）、开发流程（SD→OLED→LVGL）、构建（普通 CMake 无预设：`cmake -S . -B build -G Ninja -DCMAKE_BUILD_TYPE=Debug`→`build/lvgl_oled.elf`，零警告、UTF-8 字面量、排除 HAL *_template.c）、调试烧录、验收、常见问题表。
- **技能 `stm32-project-scaffold` 新增**：
  - 第十一节「工作空间级一键编译全部工程 `build_all.bat`」：位置（仓库根）、`call "<proj>\build_oneclick.bat" < nul` 跳过子 pause、出错才 pause 等回车继续、缺脚本标 SKIP、汇总 Passed/Failed/Skipped、cmd 老坑（`for` 内禁用 `goto`→改 `call :build` 子例程、计数器 `enabledelayedexpansion`+`!VAR!`）、验证（dummy 三态 + 真实 001 FLASH 3.13% ERRLEV=0）。
  - 第十二节「工程 README 说明文档规范」：8 个固定章节（概述/硬件/结构/流程/构建/调试烧录/验收自测/常见问题）+ 铁律（相对路径、命令与 tasks.json 一致、资源占比数字显式、随代码同步）。
  - 技能 description 触发词补充：`build_all 一键编译全部工程`、`README 工程说明文档`、`开发流程文档编写`、`工程 README 规范`。
- 两份技能副本 `diff` 校验 IDENTICAL（均 358 行）。

## 工作空间级支持包同步脚本 support_all.bat

在仓库根目录新增 `support_all.bat`，把 `support_tools/` 下的支持包按需同步进各工程（依赖补齐，配合 build_oneclick/build_all 组成完整工具链）：

- **工程→包映射**（按目录名）：含 `zephyr` → `env_support_for_zephyr`（最高优先）；首字符 `0` → `env_support_for_stm32h743`；首字符 `1` → `env_support_for_stm32f429`；`2` 开头（ESP32）无对应包 → 不处理（`for /d %%P in (0* 1*)` 只匹配 0/1 前缀）。
- **三步**：① 判定 `support_tools/env_support_for_XXX` 目录是否存在，不存在 → 解压同名 `.zip`（三个 zip 内部均包一层同名根目录，解压即得该目录）；② 解压优先 `tar -xf "<zip>" -C "<support_tools>"`，失败回退 `powershell Expand-Archive -Force`；③ 逐个比对包顶层条目（`Drivers`/`third_party`/`zephyr`），目标缺失 → `robocopy /E` 整项拷贝，已存在 → 打印 `[SKIP] <proj>\<item> already exists, skip.` 跳过（不合并、不覆盖）。
- 末尾 `pause` 等回车退出；输出行已规避 `(` `)` `&` `|` `<` `>`（沿用第十节致命坑 3）。
- **验证**：临时夹具（仅 zip 的 h743 / 预解压的 f429+zephyr / 缺失+已存在条目工程 / 2xx 跳过）跑通四分支——h743 触发解压+拷贝、zephyr 走命名分支、f429 已有条目 SKIP+缺失 COPY、201 不被处理；产物树与打印符合预期。夹具已清理。
- 技能 `stm32-project-scaffold` 新增第十三节「支持包同步脚本 `support_all.bat`」，description 触发词补充 `support_all 支持包同步`；两份副本 `diff` 校验 IDENTICAL（均 380 行）。
