# 2026-08-27

## 修复 CMakeLists 链接 + 支持包升级导致的构建断裂
- 症状：构建把 `Drivers/CMSIS/Device` 下所有 .s（arm/gcc/iar × 全 H7 型号 ~60 个）和 5 个 `system_*.c` 全收进来 → 重复 `Reset_Handler`/`SystemInit`，且 arm/iar 语法 GCC 不认。
- 修复 `CMakeLists.txt`：`CMSIS_SOURCES` 改为显式两项 —— `Source/Templates/gcc/startup_stm32h743xx.s` + `Source/Templates/system_stm32h7xx.c`。
- 附带修复（支持包 env_support_for_stm32h743 升级后目录结构变化）：
  - HAL 路径 `Drivers/HAL/{Src,Inc}` → 正确 CubeH7 布局 `Drivers/STM32H7xx_HAL_Driver/{Src,Inc}`。
  - 新增 `bsp/stm32h7xx_hal_conf.h`（从 001 工程移植）：启用 HAL/CORTEX/FLASH/GPIO/PWR/RCC/QSPI/UART/DMA/MDMA 模块；关 SD。QSPI→需 MDMA，UART→需 DMA。
  - 新增 `bsp/disk_interface.{h,c}`：新版 `third_party/FatFs/diskio.c` 要求 `disk_interface.h`。把物理盘 0（DEV_RAM）的 `RAM_disk_*` 委托到 `fs_qspi.c` 的 `qspi_disk_*`，MMC/USB 留桩，`get_fattime` 返回 0。
- 验收：Debug / Release 双构零警告通过，产出 `build/stm32h7_boot.bin`（Release 72.1KB text / Debug 110.5KB text）。
- 提醒：新增源文件后必须重新 `cmake -S . -B build` 让 GLOB 生效（否则链接报 undefined get_fattime）。

## 技能维护
- 更新 `stm32-project-scaffold` SKILL.md：在「一」修正 Drivers 布局说明（CubeH7 标准 `STM32H7xx_HAL_Driver`，旧 `Drivers/HAL` 已失效）；新增「九、CMakeLists 源收集与 third_party/Drivers 复用约束」，固化本次经验：① startup 必须显式 gcc/startup_stm32h743xx.s+system_stm32h7xx.c、禁 GLOB_RECURSE .s；② HAL 路径与 template 过滤；③ 工程自带 hal_conf.h 及 QSPI→MDMA / UART→DMA 传递依赖；④ FatFs 自带 disk_interface.{h,c} 胶水；⑤ GLOB 缓存需重 configure；⑥ 标准 H7 include 目录（含 CMSIS/Core/Include）。
