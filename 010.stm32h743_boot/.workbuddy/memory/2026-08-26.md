# 2026-08-26

## test_app 双版本升级自测包
- 在 `test_app/dist/` 下生成两套 App 升级包，每套含 `stm32h7_test.bin` + `verify.json`：
  - `dist/v1.0.0.6/`（版本槽 @0x1000 = 01 00 00 06，HMAC 52cb2ac8…）
  - `dist/v1.0.0.7/`（版本槽 @0x1000 = 01 00 00 07，HMAC e618639f…）
- 构建：Release 配置（test_app/build 已配），仅改 `app/app_version.c` 的 4 字节版本槽后 `ninja`，再用 `tools/gen_upgrade.py` 生成包。
- `verify_hmac.py` 对两套均 4/4 PASS（HMAC/长度/版本槽/密钥一致）。
- 源码 `app_version.c` 已还原默认 `1.0.0.6`，build 目录重新同步回 1.0.0.6。
- 自测用法：将 `dist/v1.0.0.7/` 内容拷到 U 盘根目录，复位即可触发 1.0.0.6→1.0.0.7 升级；串口应打印新版本号。
