# 2026-09-09

## Keil MDK-ARM 工程打通（UV4 命令行构建）
- 把本工程从 CMake/GCC 移植出可编译的 Keil 工程：`MDK-ARM/stm32h743.uvprojx` + `MDK-ARM/stm32h743.sct`（分散加载，`.framebuffer` 钉在 AXI SRAM 0x24000000，其余 RW/ZI 落 DTCM/D2/D3）
- 关键教训：`Core/Src/syscalls.c` 是 GCC/newlib 专用堆桩文件，**从 Keil 工程排除即可编译通过，不要去改它**（缺 `<sys/stat.h>` 等 POSIX 头）。已把 syscalls.c 回退为纯 GCC 版
- ARMCLANG 警告参数用**单横杠** `-Wno-...`（不是 `--Wno-`）；Cortex-M7 必须 Thumb（`uThumb=1`/`thumb=1`）
- 构建结果：`UV4.exe -b -j0 -t stm32h743` → **0 Error / 0 Warning**；Flash 90.13KB(92288B)/2MB，RAM 358.26KB(366856B，含 3 帧 240×240 帧缓冲在 AXI)
- README.md 新增 §4B Keil 构建章节；沉淀 skill 到上层目录 `D:\user_project\git\MCU-Agent\.workbuddy\skills\stm32-keil-port\SKILL.md`（与 stm32-project-scaffold 等并列）
