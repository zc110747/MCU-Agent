# 2026-09-11

## 最终帧率测试（吞吐/丢帧）收尾

项目上一待办项"最终帧率测试"完成准备与验证基础设施：

- **固件实时帧率统计**：`Core/Src/uvc_app.c` 新增 `uvc_fps_x10`（真实 fps×10，
  1 秒滑动窗口基于 `uvc_frames_sent` 增量）、`uvc_fps_window_ms`/`uvc_fps_frames`/
  `uvc_fps_ms`；`Core/Inc/uvc_app.h` 补 extern。SWD **单次可读**真实帧率，
  不打断实时 DCMI/USB 流。覆盖真实传感器与无传感器合成彩条两条路径（都在
  `tud_video_frame_xfer_complete_cb()` 递增 `uvc_frames_sent`）。
- **OpenOCD 帧率测试脚本**：`tools/fps_test.py`（纯 stdlib）。`nm` 解析符号地址
  + OpenOCD telnet 读 `uvc_fps_x10`/`uvc_frames_sent`/`uvc_frames_dropped`/
  `uvc_xfer_rejected`/`uvc_tx_timeouts`/`uvc_state`/`usb_mounted`，多次采样算
  真实吞吐 fps（Δsent/Δwall，双重校验）、丢帧率、xfer 拒绝率、tx 超时，输出
  表格 + pass/fail（退出码 0/1/2）。自启或复用 OpenOCD，自动探测 cfg。
- **双构零警告**：Debug & Release 均 arm-none-eabi-gcc 15.3.1 -Wall -Wextra 零警告。
  - Debug:   FLASH 57676 B/2MB=2.75%, RAM_D1 345600 B/512KB=65.92%, DTCM 25568 B/128KB=19.51%
  - Release: FLASH 55836 B/2MB=2.66%, RAM_D1 同 65.92%, DTCM 同 19.51%
- **附带修复**：`CMakeLists.txt` 的 `flash` target 原引用 `debug/openocd.cfg`
  （仓库实际在根 `openocd.cfg`，会导致 flash 失败），改为 `openocd.cfg`；
  README 两处 `debug/openocd.cfg` 引用同步修正。
- **README**：新增 §10 最终帧率测试（10.1 固件 fps / 10.2 脚本 / 10.3 预期结果），
  §11 验证状态勾选，原 §11 快速上手顺延 §12 并加 `python tools/fps_test.py`。

待办：真机实测帧率（无硬件，需用户烧录后运行 `python tools/fps_test.py` 把
§10.3 实测值填入 README）。

## Keil uvprojx 同步 app/bsp 重构 + UV4 编译验证

用户已把 `Core/`+`BSP/` 重构为 `app/`(应用) + `bsp/`(驱动)，并更新了 CMakeLists.txt；
本次把 `MDK-ARM/stm32h743.uvprojx` 同步到位并用 UV4 编译验证：

- **源文件组**：`Application/Startup`(sys_startup/arm 启动 + system_stm32h7xx.c)、
  `Application/User`(app/main.c、stm32h7xx_it.c、stm32h7xx_hal_msp.c、uvc_app.c、
  mdk_target.c)、`BSP`(bsp/bsp_board.c、bsp_camera.c、bsp_ov5640_ref.c、
  usb_descriptors.c)、`BSP/ov5640`、`Drivers/STM32H7xx_HAL_Driver`(19 文件)、
  `Middlewares/TinyUSB`(6 文件)。**不含 app/syscalls.c**（GCC 专用堆桩）。
- **IncludePath**：`..\app;..\bsp;..\bsp\ov5640;..\Drivers\STM32H7xx_HAL_Driver\Inc;
  ..\Drivers\STM32H7xx_HAL_Driver\Inc\Legacy;..\Drivers\CMSIS\Include;
  ..\sys_startup;..\third_party\tinyusb\src`
- **Define** 与 CMake 一致（另含 Keil 侧 `USE_PWR_LDO_SUPPLY`）。
- **UV4 编译**：`C:\Keil_v5\UV4\UV4.exe -j0 -b MDK-ARM\stm32h743.uvprojx -t stm32h743 -o MDK-ARM\keil_build.log`
  → 退出码 0，**0 Error(s) / 0 Warning(s)**（ARMCLANG V6.16）。
  `Code=84560  RO-data=2176  RW-data=345728  ZI-data=21144`；产物
  `MDK-ARM/Objects/stm32h743.axf|.hex|.bin`（bin 89568 B）。
- **README 同步**：新增 §4B Keil 章节；§9 目录结构改为 app/bsp；§5/§6/§7/§8 中
  所有 `Core/`、`BSP/ov5640`、`debug/openocd.cfg`、`linker/…ld`、
  `` `ov5640_ref.c` `` 路径改为新结构。
- 坑：uvprojx 路径变化后 Keil 全量重编；`syscalls.c` 若误加回 Keil 会因缺 POSIX
  头报错——保持排除（Keil 侧用 mdk_target.c）。
