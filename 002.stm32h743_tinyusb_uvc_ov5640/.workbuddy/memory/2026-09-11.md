# 2026-09-11

## 最终帧率测试（吞吐/丢帧）收尾

项目上一待办项"最终帧率测试"完成准备与验证基础设施：

- **固件实时帧率统计**：`Core/Src/uvc_app.c` 新增 `uvc_fps_x10`（真实 fps×10，
  1 秒滑动窗口基于 `uvc_frames_sent` 增量）、`uvc_fps_window_ms`/`uvc_fps_frames`/
  `uvc_fps_ms`；`Core/Inc/uvc_app.h` 补 extern。SWD **单次可读**真实帧率，
  不打断实时 DCMI/USB 流。覆盖真实传感器与无传感器合成彩条两条路径（都在
  `tud_video_frame_xfer_complete_cb()` 递增 `uvc_frames_sent`）。
- **OpenOCD 帧率测试脚本**：`tools/fps_test.py`（纯 stdlib）。`nm` 解析符号地址
  + OpenOCD telnet 读 `uvc_fps_x10`/`uvc_frames_sent`/`uvc_frames_dropped`/
  `uvc_xfer_rejected`/`uvc_tx_timeouts`/`uvc_state`/`usb_mounted`，多次采样算
  真实吞吐 fps（Δsent/Δwall，双重校验）、丢帧率、xfer 拒绝率、tx 超时，输出
  表格 + pass/fail（退出码 0/1/2）。自启或复用 OpenOCD，自动探测 cfg。
- **双构零警告**：Debug & Release 均 arm-none-eabi-gcc 15.3.1 -Wall -Wextra 零警告。
  - Debug:   FLASH 57676 B/2MB=2.75%, RAM_D1 345600 B/512KB=65.92%, DTCM 25568 B/128KB=19.51%
  - Release: FLASH 55836 B/2MB=2.66%, RAM_D1 同 65.92%, DTCM 同 19.51%
- **附带修复**：`CMakeLists.txt` 的 `flash` target 原引用 `debug/openocd.cfg`
  （仓库实际在根 `openocd.cfg`，会导致 flash 失败），改为 `openocd.cfg`；
  README 两处 `debug/openocd.cfg` 引用同步修正。
- **README**：新增 §10 最终帧率测试（10.1 固件 fps / 10.2 脚本 / 10.3 预期结果），
  §11 验证状态勾选，原 §11 快速上手顺延 §12 并加 `python tools/fps_test.py`。

待办：真机实测帧率（无硬件，需用户烧录后运行 `python tools/fps_test.py` 把
§10.3 实测值填入 README）。
