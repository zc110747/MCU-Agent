# 2026-08-10

## 完成：STM32H743 LVGL 菜单 + NES 模拟器工程（请求 B 收尾）

延续此前多轮迭代，完成最后两件事：

### 1. README.md（新建，替代空的 README_ref.md）
覆盖内容：硬件平台表（HSE 25MHz、SPI6 OLED 240x240、SDMMC1 `1:`、USART1 PA9/PA10、USB OTG_FS PA11/PA12 TinyUSB CDC、无实体按键）；软件架构与模块目录；内存布局（机器态→.dtcm 0x20000000、ROM→.ram_d2 0x30000000）；构建（CMake/Ninja/arm-gcc，Debug -Og 默认、Release -O2 推荐游玩）；OpenOCD 烧录与 Cortex-Debug；**完整串口命令清单**（help/status/pages/menu/open/sel/key/down/up/release/keys/rom list|load|stop|info/time/echo/reset）+ 虚拟按键名（up/down/left/right/a/b/select/start/ok/back/menu）；NES ROM 放置（`1:/NES` 主、`1:` 回退，Mapper 0/1/2/3/4/7=NROM/MMC1/UNROM/CNROM/MMC3/AxROM，≤256KB iNES）；Python 串口自测用法与覆盖；已知限制（沙箱无实机，未做实机烧录/显示/帧率验证）。

### 2. 验证状态
- `build/nes_h743.elf` 存在（2.6 MB），Release 构建此前零错误零告警，内存：DTCM 62.82%、RAM_D2 88.89%、FLASH 12.24%。
- `scripts/serial_test.py` 经 py_compile 通过，`--ports` 能列出 ST-Link VCP（COM6/COM19 等），默认 COM19 与 settings.json 一致。

### 待用户实机验证项（沙箱无法覆盖）
烧录、SPI6 OLED 显示、SD 读取 ROM、NES 实帧率（需 Release）。README 第 8 节已显式说明此限制。

### 关键技术约束（供后续复用）
- 所有 LV_FONT_MONTSERRAT_* 禁用，仅 lv_font_gbk_16 可用；CJK 需卡上 GBK 字模，ASCII 来自编译内表。
- 控制台协议：每条命令回一行 `OK `/`ERR `，便于脚本读到终止。
- ROM 加载在页面 tick 中异步进行（避免阻塞控制台）。
- 全屏 NES 页暂停 LVGL（`app_menu_is_full_screen()`），直接刷 SPI6。
