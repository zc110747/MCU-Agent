# 2026-09-04 工作日志

## CTF 索引前部常驻 RAM（resident index）— 实现 + 端到端验证

### 需求（用户原话）
> 使用 ctf-ttf 字库时，初始化 sd 卡完成后，先把 ctf 中多级映射中第一部分关于 ttf 信息存储地址的
> 偏移映射搬运到 ram 里缓存，确定占用空间多少，如果满足第一层级就一直保存在 ram 中，增加效率

### 落点
`ctf_load_resident()`：在 `ctf_open()` 之后、`ctf_verify_ttf()` 之前调用（固件 `lvgl_font_engine_init()`
同顺序）。

### 常驻三段（不加载 entry 表、不加载 TTF）
- ① TTF 表目录 `N×12B`（鸿蒙实测 N=11 → 132B；上限 `CTF_TABLE_RESIDENT_MAX=64`）。
- ② L1 平面表（2KB，本就在 `ctf_open()` 搬）。
- ③ 页表 `page_index_count×40B`：**仅当整张能放进页池才全常驻**（all-or-nothing），否则退化走块缓存。
  页池 `CTF_PAGE_POOL_PAGES=288×40=11520B`（11.25KB），留余量。

### 安全
`page_pointers_ok()` 加载时一次性证伪所有非空平面 `page_offset` 落池内且 run 不越界，使 lookup 第二跳
`page_offset - page_index_offset` 减法恒不溢出，无需每查边界判断。

### 实测（板级 banner，COM19 ST-Link VCP）
```
[FONT] index in RAM 12420 B = tables 132 + L1 2048 + pages 10240
[FONT] pages resident: 10240 B of a 11520 B pool -> NOT_FOUND costs 0 SD reads
[FONT] entries stay on card: 697512 B
```
CTF probe 中 `U+1F600`/`U+0F8FF` 等缺字 SD 列 = 0 → 缺字判定零 SD 访问成立。

### 验证闭环
- 主机端 `tools/host_test`：新增 `[2c]` 等价性测试（常驻路径 vs 降级卡路径对 0..0xFFFF 全表
  rc/glyph_id/advance/empty 逐字一致；常驻 `page_ram_hits>0` 且 `page_sd_reads==0`）。
  **7 字体全绿（SC 88 + TC×6 各 42 = 340 项）**。
- 固件双构 0 warning：
  - Debug   FLASH 341896B(16.30%) / RAM_D1 332152B(63.35%)
  - Release FLASH 345996B(16.50%) / RAM_D1 332160B(63.35%)
  - 较 CTF v1 基线 RAM_D1 +~13KB（页池 11.5KB + resident 结构/统计字段），仍余 ~187KB。
- 板级：`openocd` 烧录 `build-debug/lvgl_oled.elf` **Verified OK**，banner 见上。

### 改动文件
`Bsp/font/ctf_reader.{h,c}`（resident 加载 + `page_pointers_ok` + 查找三源）、
`Bsp/font/ctf_format.h`（`CTF_TAG` 宏）、`Bsp/font/lvgl_font.{h,c}`（页池 + `s_resident` + 统计）、
`Bsp/lv_font_provider.c`（`log_resident`）、`Application/app_main.c`（`page from RAM/from card` 统计）、
`tools/host_test/ctf_host_test.c`（[2c] 测试）。

### 注意（本机会编译坑，复用）
- MSYS2 下 `gcc`/`cc1` 不识 `/d/...` 前缀路径 → 用 `cygpath -w` 转原生 `D:/...` 再喂给编译器/链接器。
- 主机测试直接 `gcc` 编译固件真实源码（含 shim），比跑 `.bat` 更可控；`.bat` 在 Git Bash 下易无输出。
- 抓串口 banner：PowerShell 的 stdout 不回显，必须把结果 `Out-File` 到文件再 `Read`；且 `-File` 形式在本环境
  不写盘，**用 `-Command` 内联脚本**才生效。`System.IO.Ports.SerialPort` 读串口需 `AutoFlush=$true`，否则
  缓冲到 Close 才落盘。`StreamWriter` 构造即建文件。
- 本机 `openocd` 在 PATH：`/d/Software/openocd/bin/openocd`（非记忆里的 `D:\software\ST\...`）；ST-Link VCP = COM19。
