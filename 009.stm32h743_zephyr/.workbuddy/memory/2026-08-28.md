# 2026-08-28 工作日志

## stm32-project-scaffold skill 修正：致命坑 4 是假阳性

- 用户疑问：skill 已写明 bat 致命坑，为什么 freertos_emwin / 009 还会报 `此时不应有`？
- 根因澄清：skill 是**被动文档**，不会自动改写磁盘上的 .bat；freertos_emwin / 009 的 bat 当初没按规范写，文件本身违规。
- 实测修正（关键）：`001.stm32h743_tinyusb_cdc_msc/build_oneclick.bat` 保留原始 `cd /d "%~dp0"` + `for` 块内 `2>&1`，实测仍编译通过、**不崩**。
  → **致命坑 4（cd/%~dp0、2>&1）在本机 Win11 cmd 不触发报错**，是 skill 的误报。
  → 真正致崩的只有**致命坑 3：echo 文本里的 `( ) & | < >`**（REM/:: 注释里的括号也不崩）。
- 行动：
  1. 给 skill 加 `lint_bat.py`（preflight 校验器）：仅 `echo` 行特殊字符列为 ERROR，REM/cd %~dp0/2>&1 降为 INFO。
  2. `--self` 扫描 15 个 build_oneclick.bat：**0 ERROR**（freertos_emwin/009 的 echo 括号已修）。
  3. SKILL.md 致命坑 4 末尾追加「实测修正」段，明确不要再为 PIT4 改动 cd/2>&1。
- 反思：上一轮修 freertos_emwin 时按致命坑 4 改了 cd/2>&1 属**过度修复**（无害但非必要），真修复是清掉 echo 里的 `(FatFs and STemWin)` / `(Debug)` / `(Release)` 括号。
