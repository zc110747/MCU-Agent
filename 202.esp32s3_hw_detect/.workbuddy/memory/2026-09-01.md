# 2026-09-01

## ESP32-S3 硬件调试网关 (202) — Arduino CLI 迁移完成

- 从 PlatformIO 迁移到 **Arduino CLI + VS Code**（PlatformIO 被本机 `genie-trash` 守护锁定 `.platformio`，导致 `packages.lock` 权限失败与框架解包死循环；arduino-cli 用独立未保护的 `D:\software\arduino-cli\data` 编译干净通过）。
- 编译通过：`COMPILE_EXIT=0`，Flash 1,017,991 B (77%) / 1,310,720；DRAM 80,436 B (24%) / 327,680。产物在 `.build/`。
- 关键 Arduino 构建范式（跨 201/202 项目复用）：
  - 根 `.ino` 文件名须与工程目录同名（否则 arduino-cli 报 `main file missing`）。
  - 子目录 `.cpp` 不会被自动编译 → `.ino` 中统一 `#include "xxx.cpp"` 拼成单一编译单元。
  - arduino-cli 会把工程复制到 `.build/sketch/` 用不同路径解析 → `#pragma once` 失效导致重定义 → **全部头文件用 `#ifndef/#define/#endif` 守卫**。
  - FQBN: `esp32:esp32:esp32s3:PSRAM=opi,FlashSize=16M,PartitionScheme=default,UploadSpeed=921600`（core 3.3.11）。
  - 外部库：`arduino-cli lib install ArduinoJson PubSubClient WebSockets`（版本 7.4.3 / 2.8.0 / 2.7.2）。
- 交付脚本：`build_oneclick.bat`（检查 arduino-cli + 幂等装库 + compile）、`flash-esp32.bat`（参考 201 项目，自动扫 COM + monitor + DTR/RTS 复位）。
- 调试配置 `.vscode/{settings,tasks,launch}.json` 复用 201 项目路径（esp-x32/2601、xtensa-esp-elf-gdb/17.1_20260402、openocd-esp32/v0.12.0-esp32-20260424），elf=`${workspaceFolder}\.build\202.esp32s3_hw_detect.ino.elf`。
- 编译期修复清单已沉淀：constexpr IPAddress→const、补 ArduinoJson/WiFi/esp_mac/pin_config include、回调静态成员改 public、Update.write const_cast、uart_monitor 补私有声明。
- DELIVERY.md 已全量改写为 Arduino CLI 方案（目录树/§3/§5/§6/§7/§16 实测内存/§17 测试）。
