# 2026-09-03

## AP 热点名称修改（wifi-XXXX）
- 需求：AP 模式 SSID 改为固定前缀 `wifi-` + MAC 后四位。
- `config/app_config.h` `AP_SSID_PREFIX` 由 `ESP32S3-Debugger-` 改为 `wifi-`；
  文档同步：`user_document.md`、`DELIVERY.md`、`network/wifi_manager.h` 头注释 → `wifi-XXXX`。
- AP 密码（debugger123）、AP IP（192.168.4.1）不变。

## git 还原后重放 + MAC 全 0 修复（端到端完成）
- 用户为同步昨晚工作做了 git 还原，今晨 AP 名修改被抹掉 → 重新应用 5 处
  （app_config.h 前缀、wifi_manager.cpp/.h、user_document.md、DELIVERY.md），WS2812/PWM 功能代码完好。
- 真机发现 AP 名为 `wifi-0000`：core 3.3.11 下 `WiFi.macAddress()`/`softAPmacAddress()`
  在 WiFi 驱动启动前返回全 0（先 mode(AP) 再读也没用）。
- 修复：`esp_read_mac(mac, ESP_MAC_WIFI_STA)`（eFuse，与 Device ID 同源）+ `#include <esp_mac.h>`，
  见 `network/wifi_manager.cpp startAP()`。
- 烧录验证（COM22）：`AP 'wifi-A118' up @ 192.168.4.1`，与 Device ID `esp32s3-F6FFA118` 尾部一致 ✅。
- 用户新增约定：**后台编译超 5 分钟必须提醒是否终止**，已写入 skill `esp32-arduino-cli-build` §5d 并当场执行。
- git 还原注意：还原会恢复昨晚提交版的 LED/PWM 日志格式（"brightness 40" / "LEDC ready (pin selected at runtime...)"），
  与还原前会话里改的字符串不同属正常，勿误判为旧固件。
- 构建资源：Flash 1,053,434B（80%）/ DRAM 80,876B（24%）。

## 2026-09-03 补记：远端推送约定
- 提交 578e8a4（AP 热点名 wifi-XXXX）已于上午由会话推送至 GitHub 远端（6728169..578e8a4）。
- **用户新约定：git push 由用户自己处理，Agent 不再执行推送**（本地 commit 等操作仍可）。
- 记忆/日志/文档严禁记录 token、凭据等敏感信息。

## 2026-09-03 续：恢复暂停需求 → 确认已全部落地
- 核对结论：此前暂停的"网页实时状态（WebSocket 服务器推送）"已在昨晚同步的提交 01b51e7 中完整落地
  （broadcastState 400ms 快照 + 前端 applyState + debug/→bsp/ 改名 + Interface/Hardware 页），无剩余代码工作。
- 文档已同步：README Phase 9/10、user_document.md §5.3；构建零警告（Flash ~1.05MB）。
- `debug/` 目录为改名前遗留（被父仓 `**/Debug/*` gitignore 规则大小写不敏感屏蔽），无害，删除由用户处理。
- skill esp32-arduino-cli-build 补录 §5f：Debug/ 大小写 gitignore 屏蔽坑（git check-ignore -v 定位）。
- 新增 tools/check_banner.py：pyserial 自动找 ESP32 口（VID 0x303a 优先）+ RTS 复位抓启动横幅 + 4 项断言。
- 真机验证 PASS：AP wifi-A118 @192.168.4.1、WS :81 ready、全模块 ready、System Ready；MQTT down 为 AP 模式预期。

## 2026-09-03 收尾
- 功能全部落地，进入收尾：README 补 Phase 11（AP 名 wifi-XXXX + eFuse 读 MAC + git 还原重放）、
  当前状态/下一步更新为真机横幅 PASS 版；DELIVERY.md bsp 列表补 ws2812_led/pwm_output、
  §17 补真机横幅验证 PASS；prompter.md 旧 AP 前缀残留修正。
- 待办仅剩浏览器端实测 + flash-esp32.bat 双击验证 + push（用户执行）。
