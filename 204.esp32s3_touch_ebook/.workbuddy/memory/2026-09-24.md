# 2026-09-24

## CJK 字库：GBK32 改 SD 按需读 + 缓存（不再整包装 PSRAM）
- 用户指出：字库不必整包塞 PSRAM，用到时从 SD 读即可（同 STM32H7 方案）。
  结论：GBK24 留 PSRAM（1683KB），GBK32 改 SD 缓存（48KB）。
- `storage_service.*` 加 `storage_open/storage_read_at/storage_close`（FatFs VFS 全局锁串行）。
- `sd_font.*` 重写：Face 分两种持有方式——PSRAM(16/24) 与 SD-cache(32)。
  32px 字形按 `gid*128B` 现取，384 槽 LRU 缓存；冷读后稳态零 SD I/O。
- `theme` 暴露 `font_cjk_xl()/font_cjk_xl_px()`；Reader 字号梯 `CN16/CN24/CN32`，默认最大。
- 构建零警告（exit 0 / 0warn / 0err）；烧录 COM14 成功；boot 日志确认
  GBK24→1683KB PSRAM、GBK32→48KB SD-cached，干净返回 app_main。
- 本地提交 `ca41ea8`（7 文件）。push 由用户做。
- RTC 用户暂不处理（电池没电），维持软件时钟兜底现状。

## 待用户在真机验证
- 进 Reader 打开一本书，字号按钮应能在 A14/A16/A20/A28/CN/CN24/CN32 间切换；
  CN32 应显示锐利大字（整数点阵，非模糊）；首屏冷读后滚动流畅（缓存命中）。

## 一键脚本落地（用户指示：先不改字体，以 ca41ea8 为基线）
- 新约定：**测试生成文件一律放 `logs/`**（用户已处理旧产物并把 `logs` 写进 .gitignore）。
- 新增根目录 `build_oneclick.bat` 与 `flash.bat`（可选 COM 口参数）：
  - 纯 ASCII；每条退出路径 PAUSE（自动化验证用 `BAT_NOPAUSE=1` 跳过，不破坏交付契约）；
  - 复用 `tools\run_idf.bat`（激活 + 镜像 build_log.txt）；成败都会把日志按时间戳归档进 `logs\{build,flash}_YYYYMMDD_HHMMSS.log`；
  - build 成功路径用 findstr 摘要 `binary size / Project build complete`（v6 措辞，加 /I）。
- **cmd 验证实测结论**：Git Bash 里 `cmd //c "x.bat"` 依旧静默空跑（2s、无输出）；
  正确路径是 Bash 里直接 `BAT_NOPAUSE=1 ./xxx.bat`（Git Bash 经 cmd.exe 解释执行 .bat），
  判据 = 输出必须出现脚本自己的 banner。
- 验证记录：build（exit 0，ebook_lvgl.bin 0x1c1870，app 分区余 56%）；
  flash COM14 真机（exit 0，4 处 Wrote/Hash verified，21s）；失败路径 exit 2 时
  banner/PAUSE/退出码均正确（过程中遇 `ranlib: Permission denied` 瞬态文件锁，重跑即好）。
- 用户另三项指示：字库策略后续再做（文档默认 GBK32、英文也走点阵、GBK32→APP标题 /
  GBK24→注释小字 / 禁用 GBK16）——**未实施，等下一步指令**；总结 skill 剥离项目信息。
