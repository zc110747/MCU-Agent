# 2026-09-08

## ESP32-S3 CMSIS-DAP 调试探针 — Phase 1 首次成功构建

### 修复：最终链接失败（undefined reference to tud_descriptor_*）
- 根因：ESP-IDF 把组件编译成 `.a`，链接器只拉取能解析未定义符号的成员。`usb_device` 组件因
  REQUIRES tinyusb 而排在 tinyusb 之前被扫描，`usb_descriptors.c.obj`（提供 tinyusb 要求的
  `tud_descriptor_*` 回调）被丢弃 → undefined reference。同时强定义的 `__weak` 覆盖也会因此被丢，
  让 tinyusb 弱桩静默胜出（HID 收不到报告等隐患）。
- 用户指令：**不要编译成 .a 形式、最终一起全部链接**，避免 `__weak` 误链。ESP-IDF 组件天生是 .a，
  结构性正解是**最终链接用 `--whole-archive` 包裹自定义组件归档**，让所有目标文件无条件纳入。
- 实现：在 `main/CMakeLists.txt` 用 `idf_component_get_property` + `target_link_libraries`
  `-Wl,--whole-archive "$<TARGET_FILE:lib>" -Wl,--no-whole-archive"` 包裹
  usb_device/cmsis_dap/debug_engine/swd/target 五个组件（从 main 引用，避免组件自链接）。
- 移除了之前在 `usb_device.c` 加的 dummy-reference 补丁（不再需要）。

### 零警告
- 干净重建暴露 `cmsis_dap.c` 两个未使用变量告警：`retry`（set but not used）、`retry_dummy`
  （unused）。`swd_retry()` 已内部重试，这两个局部冗余。已删除声明与赋值。
- 结果：build 退出码 0，**零编译告警、零错误**。

### 验证
- `nm` 确认 elf 中 `tud_descriptor_device_cb/configuration_cb/string_cb`、`tud_hid_descriptor_report_cb`、
  `tud_hid_set_report_cb/get_report_cb/report_complete_cb/mount_cb` 均为 `T`（强定义已链接），无 `__weak` 误链。

### 构建命令
- `cd <proj> && source env.sh && "$PY" idf_runner.py build`（Git Bash）。
- 注意：完整 clean 重建（756 步）可能超过 ~7 分钟沙箱超时被 SIGTERM 打断在最终 app 链接；
  增量重建正常完成。被打断后重跑一次 `build` 即可（仅剩最终链接）。

### 待办
- 烧录 + USB 枚举 + SWD 连接等**实机验证**未做：当前无目标硬件连接。需用户接入 ESP32-S3 探针板
  与待调试目标（Cortex-M）后再按 Phase 2 起继续。

## 一键编译 / 一键下载脚本

### 新增脚本（工程根目录）
- `build.sh`：一键增量编译。`source env.sh` 后 `"$PY" idf_runner.py build "$@"`，支持 `./build.sh clean`、
  参数透传。已测：exit 0，"Project build complete"。
- `flash.sh [PORT] [BAUD]`：一键烧录，支持扫描系统串口。
  - 用 IDF 自带的 python（含 pyserial）扫描 COM 端口并打印列表；默认端口 `COM21`，若未指定且
    COM21 在列表中则自动选用；交互终端下可手动选择。
  - 端口列表打印到 stderr（终端可见），选定端口作为 stdout 末行供 bash `tail -1` 解析。
  - 烧录用 `"$PY" idf_runner.py flash -p PORT -b BAUD`（默认 460800）。
  - 已测：`./flash.sh`（无参，COM21 存在 → 自动选 COM21）→ 烧录 bootloader/partition-table/app
    三段，均 `Hash of data verified`，`Hard resetting via RTS pin... Done`，exit 0。
- 注意：`flash.sh` 末尾已打印 `检测到以下串口:` 列表（COM1/COM21 等），默认标记 `(默认)` 在 COM21。

### 关键约束（沿用）
- 脚本必须在 Git Bash 运行，`env.sh` 提供 IDF 环境（IDF_PATH/PY/PATH/PROCESSOR_ARCHITECTURE
  等），`idf_runner.py` 用 runpy 剥掉 MSYSTEM 后调用 idf.py。**不要**用 `activate.ps1`/裸 `idf.py`。
- 串口端口号会变（COM1 通信端口、COM21 CH343 下载口、COM22），脚本动态扫描避免硬编码。

## CMSIS-DAP v1 主机识别 / 驱动关键结论（已验证分析）

- 设备插上被"当成 USB HID 而非 CMSIS-DAP"是**正常**的：v1 就是 HID 类设备，设备管理器
  本就在"人体学输入设备"下。识别靠产品字符串含 "CMSIS-DAP" + HID Usage Page=0xFF00/usage=0x01。
  已核对固件 `usb_descriptors.c`：HID Report Descriptor 用 `TUD_HID_REPORT_DESC_GENERIC_INOUT`
  （usage page 0xFF00，符合）；产品/接口字符串均含 "CMSIS-DAP"；端点方向经查
  `TUD_HID_INOUT_DESCRIPTOR(_itfnum,_stridx,_boot,_rlen,_epout,_epin,...)` 宏定义确认
  OUT=0x01 / IN=0x81 正确，非 bug。`dap_info` 与命令分发覆盖 Keil 必需全部 DAP 命令。
- **WinUSB 陷阱（用户已踩）**：用户用 Zadig 把原生 USB 强装 WinUSB 后，Windows 能读出
  产品名（描述符正常），但 Keil 的 CMSIS-DAP 走 HIDAPI 必须 `hidusb.sys` → WinUSB 绑死后
  HID 通道打不开，Keil 看不到设备。
  - **正解**：v1 HID 设备驱动必须是系统自带 `hidusb.sys`（插上自动绑定），**禁止** WinUSB。
  - 还原：设备管理器卸载设备并勾"删除驱动"→ 重插 → 系统自动装回 hidusb。
  - WinUSB 仅适用于 CMSIS-DAP **v2**（Bulk 传输，需改固件：vendor class + BOS/OS 描述符 + bulk 端点）。
- 两个 USB 口要分清：原生 USB(GPIO19/20)=CMSIS-DAP HID=Keil 用的口；CH340(GPIO43/44, COM21)
  = 仅 UART 日志/烧录串口，不是调试接口。
- 已将上述写入 `doc/useage.md` 第 7 节（驱动黄金法则 / 还原步骤 / Keil 用法 / 双 USB 口区分）。
- **用户系统 = Windows 10**。Win10 上还原 WinUSB 的关键：卸载设备时务必勾"尝试删除此设备的驱动程序"，
  否则重插会因驱动排名再次自动绑回 WinUSB。也可用 Zadig 把目标设备下拉改回 "HID-compliant device"。
  已补充 Win10 步骤到 `doc/useage.md` 7.2 / 7.6（含按连接列出设备、pyOCD/OpenOCD 交叉验证、硬件 ID 排查）。

### 重大排查发现：用户把 Dell 设备误当成探针
- 用户贴出的设备：`HID\VID_413C&PID_2113&MI_01&Col02`，驱动 `hidserv.inf` —— 这是 **Dell 外设**
  （VID_413C=Dell，多接口复合设备），驱动正常，**与本探针无关**。
- **本探针硬件 ID = `VID_303A&PID_8502`（Espressif 0x303A / 0x8502）**。用户之前大概率是把 Dell 设备
  当探针去换驱动（甚至 WinUSB），真探针没动到，故 Keil 看不到。
- 已补 `doc/useage.md` 7.7：按硬件 ID（VID_303A）确认设备，拔插原生 USB 定位，别动其他 HID 设备
  （VID_413C/046D/0D8C 等）。
- 经验：排 Windows 设备问题时，**先要硬件 ID 再下结论**，不要相信"看起来像"，
  设备管理器里同名/同类设备很多。

### 探针驱动已确认为正确状态（Win10）
- 用户最终贴出真探针：`HID\VID_303A&PID_8502\...`，驱动 `input.inf` / 部分 `HID_Raw_Inst.NT`、
  类 GUID `{745a17a0-74d3-11d0-b6fe-00a0c90f57da}`（HID 类）、提供商 Microsoft 2006/10.0.19041。
  —— 这**就是 hidusb.sys 的正确状态**（v1 HID 该有的样子），无需再动。
- 结论：之前的 WinUSB 误装在 Dell 设备（VID_413C）上，探针本身驱动正确。驱动已不是瓶颈。
- 下一步（驱动正确后 Keil 仍空）：交叉验证 pyOCD/OpenOCD；最可能 Keil 的 VID/PID 白名单不认
  自定义 `0x303A/0x8502`。已补 `doc/useage.md` 7.8（正确驱动长相）/ 7.9（驱动正确后的下一层排查）。

### 固件侧根因修复：OpenOCD `CMD_INFO failed`（已完成，参考 007 项目校正）
- 用户跑 `openocd -f interface/cmsis-dap.cfg -f target/stm32h7x.cfg` 报 `Error: CMSIS-DAP command CMD_INFO failed.`
  驱动已正确（hidusb.sys），故问题在**固件 CMSIS-DAP 协议层**，不是驱动。
- **权威依据 = 参考项目 `007.stm32h743_cmsis_dap`**（完整、已测通过的 STM32H7 实现，直接调用 ARM 官方
  `DAP.c`/`DAP.h` v2.0.0）。核对其 `bsp/DAP_config.h`、`app/main.c`、`third_party/CMSIS-DAP/DAP.h` 后
  确认：**ARM 的 InfoType 键是 1-based**（DAP_ID_VENDOR=1, DAP_ID_PRODUCT=2, DAP_ID_SER_NUM=3,
  DAP_ID_FW_VER=4, DAP_ID_CAPABILITIES=0xF0, DAP_ID_PACKET_COUNT=0xFE, DAP_ID_PACKET_SIZE=0xFF）；
  **`DAP_Info` 响应格式 = `[cmd, length, data...]`**，即 `response[1]` = **payload 长度**（OpenOCD/Keil 解析它），
  数据从 `response[2]` 起。
- **此前某次"修复"是错误的**（把 `response[1]` 当 InfoType 回显、将 dap_info case 改成 0x00 起始）。
  那是错误的理解。本次以 007 项目为规范源**重新校正**：
  1. `dap_info()` switch 改为 **1-based keys**（case 1/2/3/4/0xF0/0xFF/0xFE），与 ARM DAP.h 完全对齐。
  2. `ID_DAP_Info` 分支：`response[1] = (uint8_t)num`（= 长度），返回 `(2<<16)+2+num`，数据写入 `response+2`。
     这才是 ARM 规范要求的格式，OpenOCD 才会认可。
  3. `dap_task` 取 `exec_ret & 0xFFFF`（低16位=响应长度）传给 `usb_device_send_response`，不再整段当 len。
  4. 顺手：响应缓冲 `memset` 清零（Info 字符串隐式 null 终止）；serial `snprintf` size = `DAP_PACKET_SIZE-2`；
     FW 版本 `sizeof-1` 去掉自带 '\0'；capabilities=SWD(0x01)|atomic(0x10)；packet count=1（HID v1 单缓冲）。
- 加了 COM21（UART0）调试日志：HID OUT 回调边界（`HID OUT cmd=... len=...`）、命令分发（`DAP cmd id=...`）、
  `DAP_Info id=... len=...`。可肉眼观察 OpenOCD 的完整命令流。
- 修复后 `build` 零警告零错误（exit 0），已 `flash -p COM21 -b 460800` 烧录成功（Hash verified + Hard reset Done）。
- 关键规范点：CMSIS-DAP v1 HID `DAP_Info` 响应 = `[cmd, length, data...]`；字符串类型靠缓冲零填充隐式
  null 终止；数值类型（0xF0 能力位/0xFF 包大小/0xFE 包计数）紧随 byte2。
- 请用户重跑 OpenOCD 确认：预期**不再**报 CMD_INFO failed，应进入连接目标流程（需外 MCU 接好 SWD 三线）。

## JTAG 模式实现（代码完成，待构建修复 + 实机测试）

### 背景 / 用户指令
- 之前 SWD 模式连目标返回 `ack=0x07 (NO_RESPONSE)`（nRESET 脉冲 + JTAG→SWD 切换 + swd_connect 自检仍失败，物理飞线/方向问题未定位）。
- 用户重定向：**"先验证 JTAG 模式，不切换，增加两个引脚实现 JTAG 需要引脚，先实现完成，更新到 useage.md，我连接后由你进行测试"**。
- 即 JTAG 走**纯 JTAG bring-up，不做 SWJ 切换**；SWD 问题留到 JTAG 跑通后再处理。

### 引脚分配（已确认，Kconfig 默认）
| 信号 | GPIO | 说明 |
|------|------|------|
| TCK  | 4 | 复用 SWCLK |
| TMS  | 5 | 复用 SWDIO |
| TDI  | 7 | 新增 |
| TDO  | 8 | 新增 |
| nTRST| 9 | 新增（Kconfig 默认 9，可设 -1 不接）|
| nRESET | 6 | 复用已有 SWD nRESET |
> 来源：`components/jtag/Kconfig` 默认 `TDI=7 / TDO=8 / NTRST=9`，`jtag.c` 用 `CONFIG_DEBUG_SWCLK_GPIO`(4)/`SWDIO_GPIO`(5)/`NRESET_GPIO`(6) 复用。
> JTAG 默认时钟 1MHz，max 4MHz（`DEBUG_JTAG_DEFAULT_CLOCK_HZ=1000000` / `MAX=4000000`）。

### 已完成代码（开发层面）
- 新增 `components/jtag/`：`jtag.h` / `jtag.c` / `Kconfig` / `CMakeLists.txt`（REQUIRES driver，需补 `swd`）。
  - 纯 GPIO 位带引擎，核心 I/O（`jtag_ir`/`jtag_transfer`/`jtag_read_idcode`/`jtag_write_abort`/`jtag_sequence`）
    逐字移植 ARM `JTAG_DP.c`（CMSIS-DAP v2.0.0），线协议与参考固件字节一致。
  - 暴露：`jtag_init`/`jtag_configure`/`jtag_set_device_index`/`jtag_get_count`/`jtag_connect`/
    `jtag_line_reset`/`jtag_set_clock`/`jtag_set_idle(_cycles)`，及 `jtag_pin_tck/tms/tdi/tdi_in/tdo_in/ntrst_assert/ntrst_read`。
  - 符号核对：cmsis_dap.c 引用的外部 `jtag_*` 全部在 jtag 组件中有定义（`jtag_index`/`jtag_retry`/`jtag_transfer_block`/`jtag_idcode` 均为 cmsis_dap.c 内局部，非缺失符号）。
- `components/swd/include/swd.h`：**修复 SWD_REQ 宏 bug** —— 原 `((addr8>>2)&0x0Cu)` 恒得 0，
  致除 IDCODE(0x00) 外所有 DP 寄存器读错地址（很可能是之前 SWD ack 异常的部分根因）。
  改为 `(addr8 & 0x0Cu)` 正确取 A[3:2]。
- `components/cmsis_dap/include/cmsis_dap.h`：新增 JTAG 命令 ID
  `ID_DAP_JTAG_Sequence=0x14` / `Configure=0x15` / `IDCODE=0x16` / `Transfer=0x17` / `TransferBlock=0x18`
  （外加已有 `SWJ_Sequence=0x1D`）；`DAP_PORT_JTAG=2`。**注意**：标准 OpenOCD/pyOCD 期望 0x17/0x18，
  007 参考工程把 SWO ID 复用给 JTAG，我们按标准加 0x17/0x18（未用 0x19）。
- `components/cmsis_dap/cmsis_dap.c`：
  - `dap_connect` 支持 port==2 → `jtag_init()` + `jtag_configure` + 读 IDCODE 自检（日志 `Connect: JTAG port, jtag_connect=OK/FAIL`）。
  - `dap_info` 能力位加 JTAG 位 `(1<<1)`。
  - `dap_swj_pins` 增加 TDI(bit2)/TDO(bit3)/nTRST(bit5) 驱动与回读。
  - 新增 5 个 JTAG 命令处理器（wire 格式对齐 ARM DAP.c）：`dap_jtag_sequence`/`dap_jtag_configure`/
    `dap_jtag_idcode`/`dap_jtag_transfer`/`dap_jtag_transfer_block`；
    其中 `dap_jtag_sequence` 已修 status 字节写 `response[0]` 后 `response++` 跳过（对齐 ARM）。
  - `dap_transfer`/`dap_transfer_block`/`dap_write_abort` 改为 port 感知（SWD / JTAG 分派）。
- `components/debug_engine/debug_engine.c`：`#include "jtag.h"` 且 `debug_init()` 调 `jtag_init()`。
- `main/app_main.c`：启动日志增加 JTAG 引脚打印。
- `doc/useage.md`：§1 加 JTAG 引脚表；§6 架构图加 JTAG Engine 层；§8 新增 JTAG 章节
  （引脚对照 STM32H7 JTAG：TCK→PA14, TMS→PA13, TDI→PA15, TDO→PB3, nTRST→PB4, nRESET→NRST；
  OpenOCD 测试命令见 §8）。

### 待办（回家继续，按顺序）
1. **构建必改（唯一已知阻断项）**：三个 CMakeLists 的 `REQUIRES` 都要加 `jtag`，且 `main/CMakeLists.txt`
   的 `DAP_COMPONENTS` 列表也要加 `jtag`（否则 whole-archive 包裹漏掉它，弱符号/回调可能被丢）：
   - `components/cmsis_dap/CMakeLists.txt`：`REQUIRES usb_device debug_engine swd target esp_rom jtag`
   - `components/debug_engine/CMakeLists.txt`：`REQUIRES swd esp_rom jtag`
   - `main/CMakeLists.txt`：`REQUIRES usb_device cmsis_dap jtag`；`DAP_COMPONENTS` 追加 `jtag`
2. `source env.sh && ./build.sh` 零警告构建（完整 clean 重建可能超 ~7 分钟沙箱超时被打断在最终链接，重跑一次即可）。
3. `./flash.sh` 烧录到 COM21（`sdkconfig` 会自动补 JTAG 引脚默认 7/8/9）。
4. **用户连接目标板后由 agent 测试 JTAG**：OpenOCD 命令见 `doc/useage.md` §8
   （`openocd -f interface/cmsis-dap.cfg -f target/stm32h7x.cfg -c "adapter speed 100"`）。
5. JTAG 跑通（或确认飞线同样不行）后，回头处理 SWD 模式 ack=0x07（SWD_REQ 已修，可能根因部分在此 + 飞线物理层）。

### 关键约定（别忘）
- 构建/烧录必须在 Git Bash 跑：`source env.sh && ./build.sh`（不要 activate.ps1 / 裸 idf.py）。
- 飞线连接时钟用 100kHz（SWD 路径 `swd_set_clock(100000)` 已在 `dap_connect`；JTAG 由 SWJ_Clock 设）。
- 串口端口会变，`flash.sh` 动态扫描，默认 COM21。
- 用户明确：**git push 由用户自己处理，agent 不执行远程推送**。

